diff --git a/Cargo.lock b/Cargo.lock
index 57187ce3acf..bd6d3e06f27 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -2218,30 +2218,17 @@ dependencies = [
  "build_common",
  "chrono",
  "glob",
- "introspector-macros",
  "jsonschema",
- "proc-macro2",
  "quote",
  "rand 0.8.5",
  "rustc_index",
  "serde",
  "serde_json",
- "sha2",
  "syn 2.0.106",
  "toml 0.8.23",
  "walkdir",
 ]
 
-[[package]]
-name = "introspector-macros"
-version = "0.1.0"
-dependencies = [
- "proc-macro2",
- "quote",
- "serde_json",
- "syn 2.0.106",
-]
-
 [[package]]
 name = "ipc-channel"
 version = "0.20.1"
diff --git a/compiler/introspector-collector/.cargo/config.toml b/compiler/introspector-collector/.cargo/config.toml
new file mode 100644
index 00000000000..4a075639bcc
--- /dev/null
+++ b/compiler/introspector-collector/.cargo/config.toml
@@ -0,0 +1,2 @@
+[build]
+rustflags = ["-Cprefer-dynamic"]
diff --git a/compiler/introspector-collector/Cargo.toml b/compiler/introspector-collector/Cargo.toml
index 2c774819e64..d30ec0e4056 100644
--- a/compiler/introspector-collector/Cargo.toml
+++ b/compiler/introspector-collector/Cargo.toml
@@ -3,9 +3,6 @@ name = "introspector-collector"
 version = "0.1.0"
 edition = "2024"
 
-[lib]
-crate-type = ["rlib"]
-
 [package.metadata]
 mycelial_data_path = "/mnt/data1/nix/vendor/rust/cargo2nix/submodules/mycelial-usage-data/"
 
@@ -17,9 +14,9 @@ mycelial_data_path = "/mnt/data1/nix/vendor/rust/cargo2nix/submodules/mycelial-u
 # name = "enhanced_collector"
 # path = "src/enhanced_collector.rs"
 
-# [[bin]]
-# name = "working_usage_collector"
-# path = "src/working_usage_collector.rs"
+[[bin]]
+name = "working_usage_collector"
+path = "src/working_usage_collector.rs"
 
 # [[bin]]
 # name = "working_collector"
@@ -161,31 +158,9 @@ mycelial_data_path = "/mnt/data1/nix/vendor/rust/cargo2nix/submodules/mycelial-u
 # name = "usage_compiler"
 # path = "src/bin/usage_compiler.rs"
 
-# [[bin]]
-# name = "metaprogram_weaver"
-# path = "src/bin/metaprogram_weaver.rs"
-
-# [[bin]]
-# name = "proof_program"
-# path = "src/bin/proof_program.rs"
-
-# [[bin]]
-# name = "self_mapper"
-# path = "src/bin/self_mapper.rs"
-
-# [[bin]]
-# name = "test_syn_generator"
-# path = "src/bin/test_syn_generator.rs"
-
-# [[bin]]
-# name = "runbuild"
-# path = "src/bin/runbuild.rs"
-
 [dependencies]
 serde = { version = "1.0", features = ["derive"] }
 serde_json = "1.0"
-sha2 = "0.10"
-proc-macro2 = "1.0"
 quote = "1.0"
 syn = { version = "2.0", features = ["full", "visit"] }
 jsonschema = "0.18"
@@ -197,4 +172,3 @@ build_common = { workspace = true }
 analyzeme = { workspace = true }
 toml = "0.8"
 walkdir = "2.0"
-introspector-macros = { path = "../introspector-macros" }
diff --git a/compiler/introspector-collector/README.md b/compiler/introspector-collector/README.md
index 12c04defd3e..bb643653d28 100644
--- a/compiler/introspector-collector/README.md
+++ b/compiler/introspector-collector/README.md
@@ -1,87 +1,60 @@
-# Rust Eigendecomposition System
+here is my idea:
 
-A **self-evolving compiler introspection system** that treats Rust code as mathematical objects and generates optimized analysis tools through eigendecomposition.
+1. the types are nodes.
+2. the functions between them are arrows.
+3. domain is the input.
+4. range is the output. 
 
-## The Vision
+usage(type,total programs)/usage(tyoe,some program) = the parts that some program dont use yet. 
 
-This system implements the idea that:
-1. **Types are nodes**, **functions are arrows**
-2. **Domain is input**, **range is output** 
-3. **Usage patterns** reveal the mathematical structure of Rust
-4. **Eigendecomposition** extracts the essential forms
-5. **Content-addressable** canonical forms enable perfect deduplication
+we start with syn walking. 
+then with syn we walk hir. 
+we show bijection bwtween syn and hir.
+we can lift a syn walker into hir walker. 
+we can take the usage data of hir and map it into the usage data of syn. 
 
-## Single Command System
+now we can take enums.
 
-```rust
-// build.rs
-mklang!(rust_eigen_system);
-```
+we look for functions (enum value) -> string constant  we call this label(enum,string)
+now we start with all enums of size 0,1,2,3,4,5,.....71.
+for each enum we collect its string as a set. 
+we say this is an orbit of size N.
 
-This **one macro call** generates:
-- ✅ **Usage data collection** from IPFS/Nix/cache/fresh
-- ✅ **Optimized enum switches** for all Rust types  
-- ✅ **Content-addressable** canonical forms with hashes
-- ✅ **Structured data collection** with complete field metadata
-- ✅ **JSON schemas** that perfectly mirror Rust's structure
-- ✅ **Mathematical eigenforms** for complexity analysis
+now we can find functions between those enums. some function that goes from one enum type or orbit to another .
 
-## Generated Helpers
+we start with simple ones all connections between sizes 1-1, 1-2 , 2-2, etc. 
+we classify all functions in the index.
 
-```rust
-// Auto-generated optimized switches
-let item_type = switch_itemkind2string!(item.kind);
-let def_usage = switch_defkind2string!(def_kind, {
-    DefKind::Fn => "function_call",
-    DefKind::Struct => "struct_usage"
-});
+now we have paths of lenht 2, we look for paths of functions of length 3,...8,...71. 
 
-// Structured data collection
-collect_structured!(self, "hir_items", item, rustc_hir::Item);
+So now we have all connnections between enums in a graph, we can partition it.
 
-// Perfect schema validation
-save_schema!("rust_introspection_schema.json");
-```
+we can now construct rust.
 
-## Architecture
+we say we have a language based on a single data type of 1 bit. 
+or a language of pure constants (0 bits)
 
-- **`mklang_macro.rs`** - Meta-macro that generates entire system
-- **`structured_collection.rs`** - Uniform data collection macros
-- **`schema_generator.rs`** - Auto-generated JSON schemas
-- **`rust_enum_generator.rs`** - Switch macros for all Rust enums
-- **`canonical_decl.rs`** - Content-addressable canonical forms
-- **`usage_collector.rs`** - Core collection engine (now macro-driven)
+we look at each enum type as being a part of the language. we show what code uses each type and we compose it in a graph.
+we can show that there are smaller languages of rust that can compile with a subset of the code.
+we construct rust(0), 1,.... 71 where 71 is full rustc.
 
-## Mathematical Foundation
+Now we say those are monster primes and the rustc is the monster group.
+we look for bott periodicity in 2 and 8. we apply morse thoery and harmonic analysis.
 
-The system creates a **categorical functor** from Rust's structure to mathematical eigenforms:
+Now we can show how each part of rust from 0-71 is mapped into 2 or 8 bott periodic table of complexity.
 
-```
-Rust AST/HIR → Canonical Forms → Content Hashes → Eigendecomposition → Optimized Code
-```
+Now we can assign an lmfdb function number to each part of the system. 
 
-Each declaration gets:
-- **Canonical minimal form** - `enum ItemKind{Const,Enum,Fn,Static,Struct}`
-- **Content hash** - `a1b2c3d4e5f6789a` for deduplication
-- **Usage frequency** - Data-driven optimization
-- **Eigenvalue** - Mathematical complexity measure
+Now we can label all this with macros.
+We can define a macro mklang!(rust0, 2, 3,  bit, and, or, sum, compose, lambda, apply) or something like that to
+define a minimal rust.
+We can then generate those macros and map them into the math model.
+we can connec the macros to the code via the model.
+the model numbers become executble bytecode.
 
-## Data Sources
-
-The system intelligently sources data from:
-1. **IPFS** - `RUST_USAGE_IPFS_HASH=QmHash...`
-2. **Nix Store** - `RUST_USAGE_NIX_STORE=/nix/store/...`
-3. **Local Cache** - `usage_data/cached_rust_usage.json`
-4. **Fresh Generation** - Live collection fallback
-
-## Self-Evolution
-
-The system continuously refines itself:
-1. **Collects usage data** from real Rust code
-2. **Generates optimized switches** based on frequency
-3. **Updates eigenforms** with new mathematical insights
-4. **Improves performance** through data-driven optimization
-
-This creates an **arrow pointing at Rust** that converges toward the true mathematical essence of the language through iterative eigendecomposition.
+we can construct a clifford algebra of modesl 
+a multivector of simple model size 0,..71,
+then other relations and complex features about those modesl.
+We have a vector of instances of objects and arrows between them.
 
 
diff --git a/compiler/introspector-collector/build.rs b/compiler/introspector-collector/build.rs
index 5936c235f5a..2fc166620c5 100644
--- a/compiler/introspector-collector/build.rs
+++ b/compiler/introspector-collector/build.rs
@@ -1,11 +1,63 @@
-// build.rs - Simple proof demonstration
+use std::env;
+use std::fs;
+use std::path::Path;
 
 fn main() {
-    println!("cargo:rerun-if-changed=src/");
+    println!("cargo:rerun-if-changed=usage_patterns/");
     
-    // Generate the proof program binary
-    println!("🎯 PROOF SYSTEM: Generating self-analysis binary");
+    let out_dir = env::var("OUT_DIR").unwrap();
+    let dest_path = Path::new(&out_dir).join("generated_usage_code.rs");
     
-    // The proof will be demonstrated by running the binary
-    println!("Run: cargo run --bin proof_program");
+    // Generate code from usage patterns
+    let generated_code = generate_usage_code();
+    
+    fs::write(&dest_path, generated_code).unwrap();
+    
+    println!("cargo:rustc-env=GENERATED_CODE_PATH={}", dest_path.display());
+}
+
+fn generate_usage_code() -> String {
+    let mut code = String::new();
+    
+    code.push_str("// Auto-generated usage-driven code\n");
+    code.push_str("use std::collections::HashMap;\n\n");
+    
+    // Generate usage pattern executors
+    code.push_str("pub struct UsageExecutor {\n");
+    code.push_str("    patterns: HashMap<String, Box<dyn Fn() -> String>>,\n");
+    code.push_str("}\n\n");
+    
+    code.push_str("impl UsageExecutor {\n");
+    code.push_str("    pub fn new() -> Self {\n");
+    code.push_str("        let mut patterns = HashMap::new();\n");
+    
+    // Add common syn usage patterns
+    add_syn_patterns(&mut code);
+    
+    code.push_str("        Self { patterns }\n");
+    code.push_str("    }\n\n");
+    
+    code.push_str("    pub fn execute(&self, pattern: &str) -> Option<String> {\n");
+    code.push_str("        self.patterns.get(pattern).map(|f| f())\n");
+    code.push_str("    }\n");
+    code.push_str("}\n");
+    
+    code
+}
+
+fn add_syn_patterns(code: &mut String) {
+    // Parse function pattern
+    code.push_str("        patterns.insert(\"parse_file\".to_string(), Box::new(|| {\n");
+    code.push_str("            \"syn::parse_file(&content).expect(\\\"Failed to parse\\\")\".to_string()\n");
+    code.push_str("        }));\n");
+    
+    // Visit pattern
+    code.push_str("        patterns.insert(\"visit_fn\".to_string(), Box::new(|| {\n");
+    code.push_str("            \"visitor.visit_item_fn(&func)\".to_string()\n");
+    code.push_str("        }));\n");
+    
+    // Token stream pattern
+    code.push_str("        patterns.insert(\"to_tokens\".to_string(), Box::new(|| {\n");
+    code.push_str("            \"item.to_token_stream().to_string()\".to_string()\n");
+    code.push_str("        }));\n");
 }
diff --git a/compiler/introspector-collector/content_addressed.env b/compiler/introspector-collector/content_addressed.env
deleted file mode 100644
index 13a3eb2ccb9..00000000000
--- a/compiler/introspector-collector/content_addressed.env
+++ /dev/null
@@ -1,16 +0,0 @@
-# Content-addressable build system
-
-# Use specific declaration by content hash
-export RUST_DECL_HASH="a1b2c3d4e5f6789a"  # Hash of canonical enum ItemKind{Const,Enum,Fn,Static,Struct}
-
-# Use Nix store by hash  
-export RUST_USAGE_NIX_HASH="abc123def456"   # /nix/store/abc123def456-rust-decls
-
-# Build with content-addressed data
-cargo build
-
-# The system will:
-# 1. Check if we have cached data for hash a1b2c3d4e5f6789a
-# 2. If not, try IPFS lookup by hash
-# 3. If not, try Nix store by hash  
-# 4. If not, generate fresh canonical form
diff --git a/compiler/introspector-collector/data_sources.env b/compiler/introspector-collector/data_sources.env
deleted file mode 100644
index aec0d7af431..00000000000
--- a/compiler/introspector-collector/data_sources.env
+++ /dev/null
@@ -1,13 +0,0 @@
-# Environment variables for mklang! data sources
-
-# IPFS hash for distributed Rust usage database
-export RUST_USAGE_IPFS_HASH="QmRustUsageDataV1Hash123..."
-
-# Nix store path for cached eigendata
-export RUST_USAGE_NIX_STORE="/nix/store/abc123-rust-eigendata/usage.json"
-
-# Custom data path
-export RUST_USAGE_DATA_PATH="/path/to/my/rust_analysis.json"
-
-# Build with data sources
-cargo build
diff --git a/compiler/introspector-collector/error.log b/compiler/introspector-collector/error.log
new file mode 100644
index 00000000000..931d6151bfe
--- /dev/null
+++ b/compiler/introspector-collector/error.log
@@ -0,0 +1,667 @@
+=== SAVED 4 COMPLEXITY ITEMS FOR CRATE: unicode_ident ===
+=== MANIFEST SAVED: ./test_usage_data/unicode_ident_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: unicode_ident === (20226 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (119 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (265 total usages)
+=== SAVED 20 COMPLEXITY ITEMS FOR CRATE: autocfg ===
+=== MANIFEST SAVED: ./test_usage_data/autocfg_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: autocfg === (63 total usages)
+=== SAVED 10 COMPLEXITY ITEMS FOR CRATE: smallvec ===
+=== MANIFEST SAVED: ./test_usage_data/smallvec_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: smallvec === (17 total usages)
+=== MANIFEST SAVED: ./test_usage_data/cfg_if_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: cfg_if === (0 total usages)
+=== SAVED 84 COMPLEXITY ITEMS FOR CRATE: memchr ===
+=== MANIFEST SAVED: ./test_usage_data/memchr_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: memchr === (521 total usages)
+=== SAVED 2 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (66 total usages)
+=== MANIFEST SAVED: ./test_usage_data/stable_deref_trait_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: stable_deref_trait === (0 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: itoa ===
+=== MANIFEST SAVED: ./test_usage_data/itoa_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: itoa === (3 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: scopeguard ===
+=== MANIFEST SAVED: ./test_usage_data/scopeguard_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: scopeguard === (3 total usages)
+=== SAVED 4 COMPLEXITY ITEMS FOR CRATE: pin_project_lite ===
+=== MANIFEST SAVED: ./test_usage_data/pin_project_lite_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: pin_project_lite === (4 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: futures_core ===
+=== MANIFEST SAVED: ./test_usage_data/futures_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_core === (7 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (5 total usages)
+=== SAVED 22 COMPLEXITY ITEMS FOR CRATE: log ===
+=== MANIFEST SAVED: ./test_usage_data/log_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: log === (87 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: litemap ===
+=== MANIFEST SAVED: ./test_usage_data/litemap_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: litemap === (16 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: writeable ===
+=== MANIFEST SAVED: ./test_usage_data/writeable_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: writeable === (54 total usages)
+=== SAVED 71 COMPLEXITY ITEMS FOR CRATE: bytes ===
+=== MANIFEST SAVED: ./test_usage_data/bytes_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: bytes === (298 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (4 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (4 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (4 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (1 total usages)
+=== MANIFEST SAVED: ./test_usage_data/futures_sink_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_sink === (0 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (13 total usages)
+=== SAVED 32 COMPLEXITY ITEMS FOR CRATE: ryu ===
+=== MANIFEST SAVED: ./test_usage_data/ryu_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: ryu === (1528 total usages)
+=== SAVED 89 COMPLEXITY ITEMS FOR CRATE: http ===
+=== MANIFEST SAVED: ./test_usage_data/http_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: http === (1708 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: rustc_hash ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_hash_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_hash === (3 total usages)
+=== MANIFEST SAVED: ./test_usage_data/pin_utils_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: pin_utils === (0 total usages)
+=== SAVED 5 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (115 total usages)
+=== SAVED 10 COMPLEXITY ITEMS FOR CRATE: slab ===
+=== MANIFEST SAVED: ./test_usage_data/slab_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: slab === (10 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: once_cell ===
+=== MANIFEST SAVED: ./test_usage_data/once_cell_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: once_cell === (45 total usages)
+=== SAVED 20 COMPLEXITY ITEMS FOR CRATE: futures_task ===
+=== MANIFEST SAVED: ./test_usage_data/futures_task_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_task === (37 total usages)
+=== MANIFEST SAVED: ./test_usage_data/futures_io_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_io === (0 total usages)
+=== SAVED 5 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (38 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (4 total usages)
+=== SAVED 11 COMPLEXITY ITEMS FOR CRATE: percent_encoding ===
+=== MANIFEST SAVED: ./test_usage_data/percent_encoding_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: percent_encoding === (159 total usages)
+=== SAVED 4 COMPLEXITY ITEMS FOR CRATE: build_common ===
+=== MANIFEST SAVED: ./test_usage_data/build_common_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_common === (25 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: instant ===
+=== MANIFEST SAVED: ./test_usage_data/instant_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: instant === (5 total usages)
+=== SAVED 89 COMPLEXITY ITEMS FOR CRATE: proc_macro2 ===
+=== MANIFEST SAVED: ./test_usage_data/proc_macro2_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: proc_macro2 === (526 total usages)
+=== SAVED 377 COMPLEXITY ITEMS FOR CRATE: libc ===
+=== MANIFEST SAVED: ./test_usage_data/libc_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: libc === (11514 total usages)
+=== MANIFEST SAVED: ./test_usage_data/icu_normalizer_data_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_normalizer_data === (0 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: http_body ===
+=== MANIFEST SAVED: ./test_usage_data/http_body_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: http_body === (3 total usages)
+=== MANIFEST SAVED: ./test_usage_data/icu_properties_data_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_properties_data === (0 total usages)
+=== SAVED 309 COMPLEXITY ITEMS FOR CRATE: futures_util ===
+=== MANIFEST SAVED: ./test_usage_data/futures_util_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_util === (958 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: try_lock ===
+=== MANIFEST SAVED: ./test_usage_data/try_lock_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: try_lock === (3 total usages)
+=== MANIFEST SAVED: ./test_usage_data/equivalent_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: equivalent === (0 total usages)
+=== MANIFEST SAVED: ./test_usage_data/tower_service_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tower_service === (0 total usages)
+=== SAVED 83 COMPLEXITY ITEMS FOR CRATE: hashbrown ===
+=== MANIFEST SAVED: ./test_usage_data/hashbrown_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: hashbrown === (93 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: utf8parse ===
+=== MANIFEST SAVED: ./test_usage_data/utf8parse_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: utf8parse === (5 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: form_urlencoded ===
+=== MANIFEST SAVED: ./test_usage_data/form_urlencoded_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: form_urlencoded === (30 total usages)
+=== SAVED 53 COMPLEXITY ITEMS FOR CRATE: tracing_core ===
+=== MANIFEST SAVED: ./test_usage_data/tracing_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tracing_core === (175 total usages)
+=== SAVED 26 COMPLEXITY ITEMS FOR CRATE: futures_channel ===
+=== MANIFEST SAVED: ./test_usage_data/futures_channel_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: futures_channel === (51 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: sync_wrapper ===
+=== MANIFEST SAVED: ./test_usage_data/sync_wrapper_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: sync_wrapper === (3 total usages)
+=== SAVED 99 COMPLEXITY ITEMS FOR CRATE: aho_corasick ===
+=== MANIFEST SAVED: ./test_usage_data/aho_corasick_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: aho_corasick === (561 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (119 total usages)
+=== MANIFEST SAVED: ./test_usage_data/is_terminal_polyfill_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: is_terminal_polyfill === (0 total usages)
+=== SAVED 110 COMPLEXITY ITEMS FOR CRATE: quote ===
+=== MANIFEST SAVED: ./test_usage_data/quote_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: quote === (424 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: lock_api ===
+=== MANIFEST SAVED: ./test_usage_data/lock_api_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: lock_api === (17 total usages)
+=== SAVED 19 COMPLEXITY ITEMS FOR CRATE: num_traits ===
+=== MANIFEST SAVED: ./test_usage_data/num_traits_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_traits === (200 total usages)
+=== SAVED 55 COMPLEXITY ITEMS FOR CRATE: mio ===
+=== MANIFEST SAVED: ./test_usage_data/mio_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: mio === (221 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: memmap2 ===
+=== MANIFEST SAVED: ./test_usage_data/memmap2_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: memmap2 === (7 total usages)
+=== SAVED 73 COMPLEXITY ITEMS FOR CRATE: socket2 ===
+=== MANIFEST SAVED: ./test_usage_data/socket2_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: socket2 === (214 total usages)
+=== SAVED 38 COMPLEXITY ITEMS FOR CRATE: parking_lot_core ===
+=== MANIFEST SAVED: ./test_usage_data/parking_lot_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: parking_lot_core === (294 total usages)
+=== SAVED 83 COMPLEXITY ITEMS FOR CRATE: zerocopy ===
+=== MANIFEST SAVED: ./test_usage_data/zerocopy_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerocopy === (888 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: perf_event_open_sys ===
+=== MANIFEST SAVED: ./test_usage_data/perf_event_open_sys_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: perf_event_open_sys === (1425 total usages)
+=== SAVED 11 COMPLEXITY ITEMS FOR CRATE: anstyle_parse ===
+=== MANIFEST SAVED: ./test_usage_data/anstyle_parse_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: anstyle_parse === (4113 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: want ===
+=== MANIFEST SAVED: ./test_usage_data/want_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: want === (12 total usages)
+=== SAVED 45 COMPLEXITY ITEMS FOR CRATE: httparse ===
+=== MANIFEST SAVED: ./test_usage_data/httparse_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: httparse === (242 total usages)
+=== SAVED 38 COMPLEXITY ITEMS FOR CRATE: parking_lot_core ===
+=== MANIFEST SAVED: ./test_usage_data/parking_lot_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: parking_lot_core === (294 total usages)
+=== SAVED 47 COMPLEXITY ITEMS FOR CRATE: indexmap ===
+=== MANIFEST SAVED: ./test_usage_data/indexmap_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: indexmap === (94 total usages)
+=== SAVED 2 COMPLEXITY ITEMS FOR CRATE: colorchoice ===
+=== MANIFEST SAVED: ./test_usage_data/colorchoice_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: colorchoice === (5 total usages)
+=== SAVED 9 COMPLEXITY ITEMS FOR CRATE: anstyle_query ===
+=== MANIFEST SAVED: ./test_usage_data/anstyle_query_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: anstyle_query === (15 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: utf8_iter ===
+=== MANIFEST SAVED: ./test_usage_data/utf8_iter_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: utf8_iter === (391 total usages)
+=== SAVED 142 COMPLEXITY ITEMS FOR CRATE: regex_syntax ===
+=== MANIFEST SAVED: ./test_usage_data/regex_syntax_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: regex_syntax === (147834 total usages)
+=== SAVED 14 COMPLEXITY ITEMS FOR CRATE: anstyle ===
+=== MANIFEST SAVED: ./test_usage_data/anstyle_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: anstyle === (43 total usages)
+=== SAVED 4 COMPLEXITY ITEMS FOR CRATE: tower_layer ===
+=== MANIFEST SAVED: ./test_usage_data/tower_layer_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tower_layer === (3 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: version_check ===
+=== MANIFEST SAVED: ./test_usage_data/version_check_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: version_check === (96 total usages)
+=== SAVED 2 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (47 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: atomic_waker ===
+=== MANIFEST SAVED: ./test_usage_data/atomic_waker_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: atomic_waker === (7 total usages)
+=== SAVED 26 COMPLEXITY ITEMS FOR CRATE: perf_event_open_sys ===
+=== MANIFEST SAVED: ./test_usage_data/perf_event_open_sys_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: perf_event_open_sys === (1342 total usages)
+=== SAVED 928 COMPLEXITY ITEMS FOR CRATE: syn ===
+=== MANIFEST SAVED: ./test_usage_data/syn_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: syn === (5883 total usages)
+=== SAVED 23 COMPLEXITY ITEMS FOR CRATE: num_integer ===
+=== MANIFEST SAVED: ./test_usage_data/num_integer_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_integer === (195 total usages)
+=== SAVED 17 COMPLEXITY ITEMS FOR CRATE: parking_lot ===
+=== MANIFEST SAVED: ./test_usage_data/parking_lot_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: parking_lot === (64 total usages)
+=== SAVED 427 COMPLEXITY ITEMS FOR CRATE: tokio ===
+=== MANIFEST SAVED: ./test_usage_data/tokio_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tokio === (1098 total usages)
+=== SAVED 310 COMPLEXITY ITEMS FOR CRATE: regex_automata ===
+=== MANIFEST SAVED: ./test_usage_data/regex_automata_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: regex_automata === (2014 total usages)
+=== SAVED 17 COMPLEXITY ITEMS FOR CRATE: parking_lot ===
+=== MANIFEST SAVED: ./test_usage_data/parking_lot_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: parking_lot === (64 total usages)
+=== SAVED 34 COMPLEXITY ITEMS FOR CRATE: anstream ===
+=== MANIFEST SAVED: ./test_usage_data/anstream_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: anstream === (118 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (12 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: num_complex ===
+=== MANIFEST SAVED: ./test_usage_data/num_complex_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_complex === (436 total usages)
+=== SAVED 25 COMPLEXITY ITEMS FOR CRATE: getrandom ===
+=== MANIFEST SAVED: ./test_usage_data/getrandom_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: getrandom === (105 total usages)
+=== SAVED 20 COMPLEXITY ITEMS FOR CRATE: tracing ===
+=== MANIFEST SAVED: ./test_usage_data/tracing_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tracing === (40 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (11 total usages)
+=== SAVED 11 COMPLEXITY ITEMS FOR CRATE: bitflags ===
+=== MANIFEST SAVED: ./test_usage_data/bitflags_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: bitflags === (113 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: bit_vec ===
+=== MANIFEST SAVED: ./test_usage_data/bit_vec_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: bit_vec === (25 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: clap_lex ===
+=== MANIFEST SAVED: ./test_usage_data/clap_lex_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: clap_lex === (23 total usages)
+=== MANIFEST SAVED: ./test_usage_data/num_conv_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_conv === (1824 total usages)
+=== SAVED 13 COMPLEXITY ITEMS FOR CRATE: heck ===
+=== MANIFEST SAVED: ./test_usage_data/heck_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: heck === (111 total usages)
+=== SAVED 39 COMPLEXITY ITEMS FOR CRATE: base64 ===
+=== MANIFEST SAVED: ./test_usage_data/base64_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: base64 === (342 total usages)
+=== SAVED 21 COMPLEXITY ITEMS FOR CRATE: ipnet ===
+=== MANIFEST SAVED: ./test_usage_data/ipnet_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: ipnet === (74 total usages)
+=== SAVED 7 COMPLEXITY ITEMS FOR CRATE: rustc_stable_hash ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_stable_hash_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_stable_hash === (29 total usages)
+=== SAVED 23 COMPLEXITY ITEMS FOR CRATE: strsim ===
+=== MANIFEST SAVED: ./test_usage_data/strsim_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: strsim === (218 total usages)
+=== SAVED 4 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (101 total usages)
+=== SAVED 15 COMPLEXITY ITEMS FOR CRATE: synstructure ===
+=== MANIFEST SAVED: ./test_usage_data/synstructure_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: synstructure === (97 total usages)
+=== SAVED 178 COMPLEXITY ITEMS FOR CRATE: serde_derive ===
+=== MANIFEST SAVED: ./test_usage_data/serde_derive_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: serde_derive === (3424 total usages)
+=== SAVED 32 COMPLEXITY ITEMS FOR CRATE: zerovec_derive ===
+=== MANIFEST SAVED: ./test_usage_data/zerovec_derive_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerovec_derive === (1068 total usages)
+=== SAVED 20 COMPLEXITY ITEMS FOR CRATE: displaydoc ===
+=== MANIFEST SAVED: ./test_usage_data/displaydoc_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: displaydoc === (281 total usages)
+=== SAVED 91 COMPLEXITY ITEMS FOR CRATE: num_bigint ===
+=== MANIFEST SAVED: ./test_usage_data/num_bigint_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_bigint === (1081 total usages)
+=== SAVED 13 COMPLEXITY ITEMS FOR CRATE: num_iter ===
+=== MANIFEST SAVED: ./test_usage_data/num_iter_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_iter === (14 total usages)
+=== SAVED 62 COMPLEXITY ITEMS FOR CRATE: tower ===
+=== MANIFEST SAVED: ./test_usage_data/tower_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tower === (125 total usages)
+=== SAVED 125 COMPLEXITY ITEMS FOR CRATE: hyper ===
+=== MANIFEST SAVED: ./test_usage_data/hyper_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: hyper === (358 total usages)
+=== SAVED 16 COMPLEXITY ITEMS FOR CRATE: time_core ===
+=== MANIFEST SAVED: ./test_usage_data/time_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: time_core === (9 total usages)
+=== SAVED 223 COMPLEXITY ITEMS FOR CRATE: iri_string ===
+=== MANIFEST SAVED: ./test_usage_data/iri_string_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: iri_string === (1199 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: powerfmt ===
+=== MANIFEST SAVED: ./test_usage_data/powerfmt_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: powerfmt === (7 total usages)
+=== SAVED 14 COMPLEXITY ITEMS FOR CRATE: bit_set ===
+=== MANIFEST SAVED: ./test_usage_data/bit_set_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: bit_set === (48 total usages)
+=== SAVED 2 COMPLEXITY ITEMS FOR CRATE: rustc_hashes ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_hashes_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_hashes === (2 total usages)
+=== SAVED 31 COMPLEXITY ITEMS FOR CRATE: measureme ===
+=== MANIFEST SAVED: ./test_usage_data/measureme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: measureme === (148 total usages)
+=== SAVED 89 COMPLEXITY ITEMS FOR CRATE: proc_macro2 ===
+=== MANIFEST SAVED: ./test_usage_data/proc_macro2_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: proc_macro2 === (526 total usages)
+=== SAVED 26 COMPLEXITY ITEMS FOR CRATE: getrandom ===
+=== MANIFEST SAVED: ./test_usage_data/getrandom_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: getrandom === (123 total usages)
+=== SAVED 15 COMPLEXITY ITEMS FOR CRATE: rand_core ===
+=== MANIFEST SAVED: ./test_usage_data/rand_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rand_core === (89 total usages)
+=== SAVED 37 COMPLEXITY ITEMS FOR CRATE: measureme ===
+=== MANIFEST SAVED: ./test_usage_data/measureme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: measureme === (156 total usages)
+=== SAVED 136 COMPLEXITY ITEMS FOR CRATE: clap_builder ===
+=== MANIFEST SAVED: ./test_usage_data/clap_builder_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: clap_builder === (1512 total usages)
+=== SAVED 37 COMPLEXITY ITEMS FOR CRATE: measureme ===
+=== MANIFEST SAVED: ./test_usage_data/measureme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: measureme === (156 total usages)
+=== SAVED 60 COMPLEXITY ITEMS FOR CRATE: clap_derive ===
+=== MANIFEST SAVED: ./test_usage_data/clap_derive_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: clap_derive === (2062 total usages)
+=== SAVED 11 COMPLEXITY ITEMS FOR CRATE: zerofrom_derive ===
+=== MANIFEST SAVED: ./test_usage_data/zerofrom_derive_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerofrom_derive === (177 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: yoke_derive ===
+=== MANIFEST SAVED: ./test_usage_data/yoke_derive_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: yoke_derive === (271 total usages)
+=== SAVED 152 COMPLEXITY ITEMS FOR CRATE: serde ===
+=== MANIFEST SAVED: ./test_usage_data/serde_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: serde === (334 total usages)
+=== SAVED 9 COMPLEXITY ITEMS FOR CRATE: num_rational ===
+=== MANIFEST SAVED: ./test_usage_data/num_rational_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_rational === (147 total usages)
+=== SAVED 121 COMPLEXITY ITEMS FOR CRATE: hyper_util ===
+=== MANIFEST SAVED: ./test_usage_data/hyper_util_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: hyper_util === (3500 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: deranged ===
+=== MANIFEST SAVED: ./test_usage_data/deranged_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: deranged === (27 total usages)
+=== SAVED 40 COMPLEXITY ITEMS FOR CRATE: tower_http ===
+=== MANIFEST SAVED: ./test_usage_data/tower_http_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tower_http === (84 total usages)
+=== SAVED 85 COMPLEXITY ITEMS FOR CRATE: time_macros ===
+=== MANIFEST SAVED: ./test_usage_data/time_macros_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: time_macros === (835 total usages)
+=== SAVED 25 COMPLEXITY ITEMS FOR CRATE: ppv_lite86 ===
+=== MANIFEST SAVED: ./test_usage_data/ppv_lite86_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: ppv_lite86 === (32 total usages)
+=== SAVED 24 COMPLEXITY ITEMS FOR CRATE: http_body_util ===
+=== MANIFEST SAVED: ./test_usage_data/http_body_util_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: http_body_util === (60 total usages)
+=== SAVED 1 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (4 total usages)
+=== SAVED 234 COMPLEXITY ITEMS FOR CRATE: nom ===
+=== MANIFEST SAVED: ./test_usage_data/nom_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: nom === (1352 total usages)
+=== SAVED 9 COMPLEXITY ITEMS FOR CRATE: byteorder ===
+=== MANIFEST SAVED: ./test_usage_data/byteorder_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: byteorder === (48 total usages)
+=== SAVED 210 COMPLEXITY ITEMS FOR CRATE: winnow ===
+=== MANIFEST SAVED: ./test_usage_data/winnow_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: winnow === (913 total usages)
+=== SAVED 9 COMPLEXITY ITEMS FOR CRATE: toml_write ===
+=== MANIFEST SAVED: ./test_usage_data/toml_write_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: toml_write === (157 total usages)
+=== SAVED 2 COMPLEXITY ITEMS FOR CRATE: lazy_static ===
+=== MANIFEST SAVED: ./test_usage_data/lazy_static_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: lazy_static === (1 total usages)
+=== SAVED 13 COMPLEXITY ITEMS FOR CRATE: thin_vec ===
+=== MANIFEST SAVED: ./test_usage_data/thin_vec_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: thin_vec === (42 total usages)
+=== SAVED 40 COMPLEXITY ITEMS FOR CRATE: anyhow ===
+=== MANIFEST SAVED: ./test_usage_data/anyhow_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: anyhow === (98 total usages)
+=== SAVED 116 COMPLEXITY ITEMS FOR CRATE: rustc_macros ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_macros_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_macros === (2146 total usages)
+=== SAVED 58 COMPLEXITY ITEMS FOR CRATE: fancy_regex ===
+=== MANIFEST SAVED: ./test_usage_data/fancy_regex_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: fancy_regex === (274 total usages)
+=== MANIFEST SAVED: ./test_usage_data/clap_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: clap === (0 total usages)
+=== SAVED 110 COMPLEXITY ITEMS FOR CRATE: quote ===
+=== MANIFEST SAVED: ./test_usage_data/quote_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: quote === (424 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: regex ===
+=== MANIFEST SAVED: ./test_usage_data/regex_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: regex === (47 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: rustc_index_macros ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_index_macros_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_index_macros === (14 total usages)
+=== MANIFEST SAVED: ./test_usage_data/zerofrom_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerofrom === (0 total usages)
+=== SAVED 107 COMPLEXITY ITEMS FOR CRATE: serde_json ===
+=== MANIFEST SAVED: ./test_usage_data/serde_json_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: serde_json === (551 total usages)
+=== MANIFEST SAVED: ./test_usage_data/num_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num === (0 total usages)
+=== SAVED 14 COMPLEXITY ITEMS FOR CRATE: toml_datetime ===
+=== MANIFEST SAVED: ./test_usage_data/toml_datetime_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: toml_datetime === (29 total usages)
+=== SAVED 24 COMPLEXITY ITEMS FOR CRATE: serde_urlencoded ===
+=== MANIFEST SAVED: ./test_usage_data/serde_urlencoded_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: serde_urlencoded === (41 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: serde_spanned ===
+=== MANIFEST SAVED: ./test_usage_data/serde_spanned_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: serde_spanned === (13 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: rand_chacha ===
+=== MANIFEST SAVED: ./test_usage_data/rand_chacha_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rand_chacha === (209 total usages)
+=== SAVED 156 COMPLEXITY ITEMS FOR CRATE: time ===
+=== MANIFEST SAVED: ./test_usage_data/time_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: time === (1074 total usages)
+=== SAVED 18 COMPLEXITY ITEMS FOR CRATE: ahash ===
+=== MANIFEST SAVED: ./test_usage_data/ahash_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: ahash === (73 total usages)
+=== SAVED 44 COMPLEXITY ITEMS FOR CRATE: iso8601 ===
+=== MANIFEST SAVED: ./test_usage_data/iso8601_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: iso8601 === (112 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: rustc_serialize ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_serialize_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_serialize === (80 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: build_script_build ===
+=== MANIFEST SAVED: ./test_usage_data/build_script_build_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: build_script_build === (32 total usages)
+=== SAVED 9 COMPLEXITY ITEMS FOR CRATE: iana_time_zone ===
+=== MANIFEST SAVED: ./test_usage_data/iana_time_zone_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: iana_time_zone === (127 total usages)
+=== SAVED 19 COMPLEXITY ITEMS FOR CRATE: bytecount ===
+=== MANIFEST SAVED: ./test_usage_data/bytecount_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: bytecount === (240 total usages)
+=== MANIFEST SAVED: ./test_usage_data/num_cmp_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: num_cmp === (12 total usages)
+=== SAVED 33 COMPLEXITY ITEMS FOR CRATE: uuid ===
+=== MANIFEST SAVED: ./test_usage_data/uuid_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: uuid === (152 total usages)
+=== SAVED 737 COMPLEXITY ITEMS FOR CRATE: syn ===
+=== MANIFEST SAVED: ./test_usage_data/syn_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: syn === (5183 total usages)
+=== SAVED 17 COMPLEXITY ITEMS FOR CRATE: glob ===
+=== MANIFEST SAVED: ./test_usage_data/glob_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: glob === (150 total usages)
+=== SAVED 5 COMPLEXITY ITEMS FOR CRATE: yoke ===
+=== MANIFEST SAVED: ./test_usage_data/yoke_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: yoke === (11 total usages)
+=== SAVED 183 COMPLEXITY ITEMS FOR CRATE: toml_edit ===
+=== MANIFEST SAVED: ./test_usage_data/toml_edit_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: toml_edit === (1653 total usages)
+=== SAVED 18 COMPLEXITY ITEMS FOR CRATE: decodeme ===
+=== MANIFEST SAVED: ./test_usage_data/decodeme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: decodeme === (86 total usages)
+=== SAVED 18 COMPLEXITY ITEMS FOR CRATE: decodeme ===
+=== MANIFEST SAVED: ./test_usage_data/decodeme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: decodeme === (83 total usages)
+=== SAVED 30 COMPLEXITY ITEMS FOR CRATE: analyzeme ===
+=== MANIFEST SAVED: ./test_usage_data/analyzeme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: analyzeme === (226 total usages)
+=== SAVED 27 COMPLEXITY ITEMS FOR CRATE: fraction ===
+=== MANIFEST SAVED: ./test_usage_data/fraction_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: fraction === (586 total usages)
+=== SAVED 44 COMPLEXITY ITEMS FOR CRATE: rand ===
+=== MANIFEST SAVED: ./test_usage_data/rand_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rand === (187 total usages)
+=== SAVED 30 COMPLEXITY ITEMS FOR CRATE: rustc_index ===
+=== MANIFEST SAVED: ./test_usage_data/rustc_index_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: rustc_index === (91 total usages)
+   Compiling introspector-collector v0.1.0 (/mnt/data1/nix/vendor/rust/cargo2nix/submodules/rust/compiler/introspector-collector)
+=== SAVED 156 COMPLEXITY ITEMS FOR CRATE: chrono ===
+=== MANIFEST SAVED: ./test_usage_data/chrono_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: chrono === (5790 total usages)
+=== SAVED 50 COMPLEXITY ITEMS FOR CRATE: zerovec ===
+=== MANIFEST SAVED: ./test_usage_data/zerovec_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerovec === (132 total usages)
+=== SAVED 37 COMPLEXITY ITEMS FOR CRATE: zerotrie ===
+=== MANIFEST SAVED: ./test_usage_data/zerotrie_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: zerotrie === (143 total usages)
+=== SAVED 43 COMPLEXITY ITEMS FOR CRATE: toml ===
+=== MANIFEST SAVED: ./test_usage_data/toml_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: toml === (106 total usages)
+=== SAVED 29 COMPLEXITY ITEMS FOR CRATE: analyzeme ===
+=== MANIFEST SAVED: ./test_usage_data/analyzeme_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: analyzeme === (241 total usages)
+=== SAVED 6 COMPLEXITY ITEMS FOR CRATE: tinystr ===
+=== MANIFEST SAVED: ./test_usage_data/tinystr_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: tinystr === (7 total usages)
+=== SAVED 3 COMPLEXITY ITEMS FOR CRATE: potential_utf ===
+=== MANIFEST SAVED: ./test_usage_data/potential_utf_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: potential_utf === (3 total usages)
+=== SAVED 42 COMPLEXITY ITEMS FOR CRATE: icu_locale_core ===
+=== MANIFEST SAVED: ./test_usage_data/icu_locale_core_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_locale_core === (105 total usages)
+=== SAVED 25 COMPLEXITY ITEMS FOR CRATE: icu_collections ===
+=== MANIFEST SAVED: ./test_usage_data/icu_collections_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_collections === (4828 total usages)
+=== SAVED 30 COMPLEXITY ITEMS FOR CRATE: icu_provider ===
+=== MANIFEST SAVED: ./test_usage_data/icu_provider_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_provider === (60 total usages)
+=== SAVED 244 COMPLEXITY ITEMS FOR CRATE: icu_properties ===
+=== MANIFEST SAVED: ./test_usage_data/icu_properties_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_properties === (660 total usages)
+=== SAVED 46 COMPLEXITY ITEMS FOR CRATE: icu_normalizer ===
+=== MANIFEST SAVED: ./test_usage_data/icu_normalizer_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: icu_normalizer === (289 total usages)
+=== SAVED 8 COMPLEXITY ITEMS FOR CRATE: idna_adapter ===
+=== MANIFEST SAVED: ./test_usage_data/idna_adapter_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: idna_adapter === (129 total usages)
+=== SAVED 45 COMPLEXITY ITEMS FOR CRATE: idna ===
+=== MANIFEST SAVED: ./test_usage_data/idna_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: idna === (270 total usages)
+=== SAVED 65 COMPLEXITY ITEMS FOR CRATE: url ===
+=== MANIFEST SAVED: ./test_usage_data/url_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: url === (492 total usages)
+=== SAVED 123 COMPLEXITY ITEMS FOR CRATE: reqwest ===
+=== MANIFEST SAVED: ./test_usage_data/reqwest_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: reqwest === (402 total usages)
+=== SAVED 172 COMPLEXITY ITEMS FOR CRATE: jsonschema ===
+=== MANIFEST SAVED: ./test_usage_data/jsonschema_manifest.json ===
+=== COLLECTING USAGE DATA FOR CRATE: jsonschema === (1008 total usages)
+warning: unused import: `std::io::Write`
+ --> compiler/introspector-collector/src/file_manager.rs:3:5
+  |
+3 | use std::io::Write;
+  |     ^^^^^^^^^^^^^^
+  |
+  = note: `#[warn(unused_imports)]` (part of `#[warn(unused)]`) on by default
+
+warning: unused import: `crate::data_structures::*`
+ --> compiler/introspector-collector/src/visitors.rs:7:5
+  |
+7 | use crate::data_structures::*;
+  |     ^^^^^^^^^^^^^^^^^^^^^^^^^
+
+warning: unused import: `std::collections::HashMap`
+  --> compiler/introspector-collector/src/visitors.rs:10:5
+   |
+10 | use std::collections::HashMap;
+   |     ^^^^^^^^^^^^^^^^^^^^^^^^^
+
+warning: unused import: `crate::collectors::*`
+  --> compiler/introspector-collector/src/usage_collector.rs:11:5
+   |
+11 | use crate::collectors::*;
+   |     ^^^^^^^^^^^^^^^^^^^^
+
+warning: unused imports: `Visitor` and `self`
+  --> compiler/introspector-collector/src/usage_collector.rs:17:29
+   |
+17 | use rustc_hir::intravisit::{self, Visitor};
+   |                             ^^^^  ^^^^^^^
+
+warning: crate-level attribute should be in the root module
+ --> compiler/introspector-collector/src/visitors.rs:1:1
+  |
+1 | #![feature(rustc_private)]
+  | ^^^^^^^^^^^^^^^^^^^^^^^^^^
+  |
+  = note: `#[warn(unused_attributes)]` (part of `#[warn(unused)]`) on by default
+
+warning: crate-level attribute should be in the root module
+ --> compiler/introspector-collector/src/usage_collector.rs:1:1
+  |
+1 | #![feature(rustc_private)]
+  | ^^^^^^^^^^^^^^^^^^^^^^^^^^
+
+warning: unused import: `Visitor`
+ --> compiler/introspector-collector/src/visitors.rs:9:35
+  |
+9 | use rustc_hir::intravisit::{self, Visitor};
+  |                                   ^^^^^^^
+
+warning: unused variable: `enum_data`
+  --> compiler/introspector-collector/src/file_manager.rs:10:9
+   |
+10 |         enum_data: &HashMap<String, EnumInfo>,
+   |         ^^^^^^^^^ help: if this is intentional, prefix it with an underscore: `_enum_data`
+   |
+   = note: `#[warn(unused_variables)]` (part of `#[warn(unused)]`) on by default
+
+warning: unused variable: `body_id`
+   --> compiler/introspector-collector/src/usage_collector.rs:203:53
+    |
+203 |                 rustc_hir::ItemKind::Const(_, _, _, body_id) => {
+    |                                                     ^^^^^^^ help: if this is intentional, prefix it with an underscore: `_body_id`
+
+warning: unused variable: `user_location`
+   --> compiler/introspector-collector/src/usage_collector.rs:278:17
+    |
+278 |             let user_location = format!("crate::{}::{}::{}", local_crate, module, decl);
+    |                 ^^^^^^^^^^^^^ help: if this is intentional, prefix it with an underscore: `_user_location`
+
+warning: `introspector-collector` (lib) generated 11 warnings (run `cargo fix --lib -p introspector-collector` to apply 5 suggestions)
+warning: struct `CleanGraphData` is never constructed
+ --> compiler/introspector-collector/src/data_structures.rs:5:12
+  |
+5 | pub struct CleanGraphData {
+  |            ^^^^^^^^^^^^^^
+  |
+  = note: `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default
+
+warning: struct `LiteralVisitor` is never constructed
+  --> compiler/introspector-collector/src/visitors.rs:13:12
+   |
+13 | pub struct LiteralVisitor<'a> {
+   |            ^^^^^^^^^^^^^^
+
+warning: struct `TypeUsageVisitor` is never constructed
+   --> compiler/introspector-collector/src/visitors.rs:181:12
+    |
+181 | pub struct TypeUsageVisitor<'a, 'tcx> {
+    |            ^^^^^^^^^^^^^^^^
+
+warning: methods `classify_usage`, `extract_enum_variant`, and `update_enum_usage` are never used
+   --> compiler/introspector-collector/src/usage_collector.rs:90:12
+    |
+ 27 | impl UsageCollector {
+    | ------------------- methods in this implementation
+...
+ 90 |     pub fn classify_usage(&self, usage: &str, usage_type: &str, _used_def_id: &str) -> UsageClassification {
+    |            ^^^^^^^^^^^^^^
+...
+119 |     pub fn extract_enum_variant(&self, def_id: &str) -> Option<(String, String)> {
+    |            ^^^^^^^^^^^^^^^^^^^^
+...
+138 |     pub fn update_enum_usage(&mut self, enum_name: String, variant_name: String, 
+    |            ^^^^^^^^^^^^^^^^^
+
+warning: `introspector-collector` (bin "introspector-collector") generated 15 warnings (11 duplicates)
+warning: `introspector-collector` (bin "working_usage_collector") generated 15 warnings (15 duplicates)
+    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.90s
diff --git a/compiler/introspector-collector/self_analyze.sh b/compiler/introspector-collector/self_analyze.sh
deleted file mode 100755
index 0555ab1097d..00000000000
--- a/compiler/introspector-collector/self_analyze.sh
+++ /dev/null
@@ -1,22 +0,0 @@
-#!/bin/bash
-# Self-analysis: Apply collector to itself and map to Rust elements
-
-echo "=== SELF-ANALYSIS: Collector → eigen(Rust) ==="
-
-# Step 1: Collect usage data from our own codebase
-echo "Step 1: Collecting self-usage data..."
-RUSTC_WRAPPER=./target/debug/working_usage_collector cargo build --lib 2>/dev/null
-
-# Step 2: Read existing libusagedata 
-echo "Step 2: Loading existing Rust usage patterns..."
-if [ -f "usage_data/rust_core_usage.json" ]; then
-    echo "Found existing usage data"
-else
-    echo "No existing data - this will be our baseline"
-fi
-
-# Step 3: Map our elements to Rust elements
-echo "Step 3: Mapping collector elements to Rust eigenspace..."
-cargo run --bin self_mapper 2>/dev/null || echo "Building self-mapper..."
-
-echo "=== Self-analysis complete ==="
diff --git a/compiler/introspector-collector/src/bin/metaprogram_weaver.rs b/compiler/introspector-collector/src/bin/metaprogram_weaver.rs
deleted file mode 100644
index b72619ec238..00000000000
--- a/compiler/introspector-collector/src/bin/metaprogram_weaver.rs
+++ /dev/null
@@ -1,29 +0,0 @@
-use introspector_collector::{
-    typespace_mapper::TypespaceMapper,
-    metaprogram_generator::MetaprogramGenerator,
-};
-
-fn main() -> Result<(), Box<dyn std::error::Error>> {
-    println!("=== METAPROGRAM WEAVER: Generating rustmap! macros ===");
-    
-    // Step 1: Load typespace mappings
-    let mappings_json = std::fs::read_to_string("typespace_mappings.json")
-        .unwrap_or_else(|_| "[]".to_string());
-    let mappings: Vec<introspector_collector::typespace_mapper::TypespaceMapping> = 
-        serde_json::from_str(&mappings_json)?;
-    
-    println!("Loaded {} typespace mappings", mappings.len());
-    
-    // Step 2: Generate rustmap! macros and projections
-    let mut generator = MetaprogramGenerator::new();
-    generator.generate_rustmaps(&mappings);
-    
-    // Step 3: Save the generated metaprogram
-    generator.save_generated_code("src/generated_rustmaps.rs")?;
-    
-    println!("\n=== WEAVING COMPLETE ===");
-    println!("Visitor transformed into projection/weave system");
-    println!("Types lifted into mathematical eigenforms");
-    
-    Ok(())
-}
diff --git a/compiler/introspector-collector/src/bin/proof_program.rs b/compiler/introspector-collector/src/bin/proof_program.rs
deleted file mode 100644
index 0dafae52e15..00000000000
--- a/compiler/introspector-collector/src/bin/proof_program.rs
+++ /dev/null
@@ -1,40 +0,0 @@
-use introspector_collector::proof_program::ProofProgram;
-
-fn main() -> Result<(), Box<dyn std::error::Error>> {
-    println!("🎯 THE PROOF PROGRAM");
-    println!("Self-analyzing system: What's duplicated, missing, and truly novel");
-    println!("=====================================");
-    
-    let mut proof = ProofProgram::new();
-    
-    // Step 1: Read our own codebase
-    proof.read_self()?;
-    
-    // Step 2: Read experimental codebase  
-    proof.read_experiments()?;
-    
-    // Step 3: Load Rust's canonical model
-    proof.load_rust_model()?;
-    
-    // Step 4: Generate the proof analysis
-    let analysis = proof.generate_proof();
-    
-    // Step 5: Save results
-    proof.save_proof(&analysis, "proof_analysis.json")?;
-    
-    // Step 6: Print summary
-    println!("\n🎉 PROOF COMPLETE!");
-    println!("📊 Duplicated from Rust: {}", analysis.duplicated_from_rust.len());
-    println!("🚫 Missing from Rust: {}", analysis.missing_from_rust.len());
-    println!("✨ Truly Novel: {}", analysis.truly_novel.len());
-    println!("💡 Improvement Suggestions: {}", analysis.improvement_suggestions.len());
-    println!("🔄 Rewrite Recommendations: {}", analysis.rewrite_recommendations.len());
-    
-    println!("\n📝 Reports generated:");
-    println!("   - proof_analysis.json (machine-readable)");
-    println!("   - proof_analysis_report.md (human-readable)");
-    
-    println!("\n🎯 The proof is complete. The arrow has analyzed itself.");
-    
-    Ok(())
-}
diff --git a/compiler/introspector-collector/src/bin/runbuild.rs b/compiler/introspector-collector/src/bin/runbuild.rs
deleted file mode 100644
index f69ff1b2179..00000000000
--- a/compiler/introspector-collector/src/bin/runbuild.rs
+++ /dev/null
@@ -1,37 +0,0 @@
-use std::process::Command;
-
-fn main() {
-    println!("🔧 Running build script standalone...");
-    
-    // Run the build script directly
-    let output = Command::new("rustc")
-        .args(&["--edition", "2021", "build.rs", "-o", "build_runner"])
-        .output()
-        .expect("Failed to compile build script");
-    
-    if !output.status.success() {
-        eprintln!("Build script compilation failed:");
-        eprintln!("{}", String::from_utf8_lossy(&output.stderr));
-        return;
-    }
-    
-    println!("✅ Build script compiled, running...");
-    
-    // Execute the compiled build script
-    let build_output = Command::new("./build_runner")
-        .output()
-        .expect("Failed to run build script");
-    
-    println!("Build script output:");
-    println!("{}", String::from_utf8_lossy(&build_output.stdout));
-    
-    if !build_output.stderr.is_empty() {
-        println!("Build script stderr:");
-        println!("{}", String::from_utf8_lossy(&build_output.stderr));
-    }
-    
-    // Clean up
-    std::fs::remove_file("build_runner").ok();
-    
-    println!("🎯 Build script execution complete!");
-}
diff --git a/compiler/introspector-collector/src/bin/self_mapper.rs b/compiler/introspector-collector/src/bin/self_mapper.rs
deleted file mode 100644
index d7242c3d67b..00000000000
--- a/compiler/introspector-collector/src/bin/self_mapper.rs
+++ /dev/null
@@ -1,35 +0,0 @@
-use introspector_collector::self_mapper::SelfMapper;
-use std::env;
-
-fn main() -> Result<(), Box<dyn std::error::Error>> {
-    let args: Vec<String> = env::args().collect();
-    
-    println!("=== SELF-REFLECTION: Collector analyzing itself ===");
-    
-    let mut mapper = SelfMapper::new();
-    
-    // Load our own usage data (generated by applying collector to itself)
-    let self_usage_path = "usage_data/introspector_collector_usage.json";
-    if let Err(e) = mapper.load_self_usage(self_usage_path) {
-        println!("Warning: Could not load self-usage data: {}", e);
-        println!("Run: RUSTC_WRAPPER=./target/debug/working_usage_collector cargo build --lib");
-    }
-    
-    // Load existing Rust usage data from libusagedata
-    let rust_usage_path = "usage_data/rust_core_usage.json";
-    if let Err(e) = mapper.load_rust_usage(rust_usage_path) {
-        println!("Warning: Could not load Rust usage data: {}", e);
-        println!("This will be our baseline measurement");
-    }
-    
-    // Perform the mapping: Collector → Rust eigenspace
-    mapper.map_to_rust_eigenspace();
-    
-    // Generate eigendecomposition analysis
-    mapper.generate_eigendecomposition();
-    
-    println!("\n=== SELF-ANALYSIS COMPLETE ===");
-    println!("The arrow has been refined through self-reflection.");
-    
-    Ok(())
-}
diff --git a/compiler/introspector-collector/src/bin/test_syn_generator.rs b/compiler/introspector-collector/src/bin/test_syn_generator.rs
deleted file mode 100644
index 56dabde64ed..00000000000
--- a/compiler/introspector-collector/src/bin/test_syn_generator.rs
+++ /dev/null
@@ -1,21 +0,0 @@
-use introspector_collector::syn_macro_generator::SynMacroGenerator;
-
-fn main() {
-    println!("🚀 Testing syn-based macro generation...");
-    
-    let mut generator = SynMacroGenerator::new();
-    
-    // Generate all enum switch macros
-    generator.generate_all_rust_enum_macros();
-    
-    // Save to a test file
-    match generator.save_generated_macros("test_syn_macros.rs") {
-        Ok(()) => {
-            println!("✅ Successfully generated macros!");
-            println!("📁 Check test_syn_macros.rs to see the generated code");
-        }
-        Err(e) => {
-            println!("❌ Error generating macros: {}", e);
-        }
-    }
-}
diff --git a/compiler/introspector-collector/src/canonical_decl.rs b/compiler/introspector-collector/src/canonical_decl.rs
deleted file mode 100644
index 3accc75f17b..00000000000
--- a/compiler/introspector-collector/src/canonical_decl.rs
+++ /dev/null
@@ -1,143 +0,0 @@
-use sha2::{Sha256, Digest};
-use serde::{Serialize, Deserialize};
-use std::collections::HashMap;
-
-#[derive(Serialize, Deserialize, Debug, Clone)]
-pub struct CanonicalDecl {
-    pub hash: String,           // Content hash of canonical form
-    pub minimal_form: String,   // Canonical minimal representation
-    pub decl_type: String,      // "enum", "struct", "fn", etc.
-    pub name: String,           // Declaration name
-    pub variants: Vec<String>,  // For enums: variant names
-    pub fields: Vec<String>,    // For structs: field names  
-    pub signature: String,      // For functions: minimal signature
-}
-
-pub struct DeclCanonicalizer {
-    cache: HashMap<String, CanonicalDecl>,
-}
-
-impl DeclCanonicalizer {
-    pub fn new() -> Self {
-        Self {
-            cache: HashMap::new(),
-        }
-    }
-    
-    pub fn canonicalize_enum(&mut self, name: &str, variants: &[&str]) -> CanonicalDecl {
-        // Create canonical minimal form
-        let mut sorted_variants: Vec<_> = variants.iter().map(|s| s.to_string()).collect();
-        sorted_variants.sort();
-        
-        let minimal_form = format!("enum {}{{{}}}", name, sorted_variants.join(","));
-        let hash = self.content_hash(&minimal_form);
-        
-        let canonical = CanonicalDecl {
-            hash: hash.clone(),
-            minimal_form,
-            decl_type: "enum".to_string(),
-            name: name.to_string(),
-            variants: sorted_variants,
-            fields: vec![],
-            signature: String::new(),
-        };
-        
-        self.cache.insert(hash, canonical.clone());
-        canonical
-    }
-    
-    pub fn canonicalize_struct(&mut self, name: &str, fields: &[&str]) -> CanonicalDecl {
-        let mut sorted_fields: Vec<_> = fields.iter().map(|s| s.to_string()).collect();
-        sorted_fields.sort();
-        
-        let minimal_form = format!("struct {}{{{}}}",  name, sorted_fields.join(","));
-        let hash = self.content_hash(&minimal_form);
-        
-        let canonical = CanonicalDecl {
-            hash: hash.clone(),
-            minimal_form,
-            decl_type: "struct".to_string(),
-            name: name.to_string(),
-            variants: vec![],
-            fields: sorted_fields,
-            signature: String::new(),
-        };
-        
-        self.cache.insert(hash, canonical.clone());
-        canonical
-    }
-    
-    pub fn canonicalize_function(&mut self, name: &str, params: &[&str], return_type: &str) -> CanonicalDecl {
-        let signature = format!("fn {}({}) -> {}", name, params.join(","), return_type);
-        let minimal_form = self.minimize_signature(&signature);
-        let hash = self.content_hash(&minimal_form);
-        
-        let canonical = CanonicalDecl {
-            hash: hash.clone(),
-            minimal_form,
-            decl_type: "fn".to_string(),
-            name: name.to_string(),
-            variants: vec![],
-            fields: vec![],
-            signature,
-        };
-        
-        self.cache.insert(hash, canonical.clone());
-        canonical
-    }
-    
-    fn content_hash(&self, content: &str) -> String {
-        let mut hasher = Sha256::new();
-        hasher.update(content.as_bytes());
-        format!("{:x}", hasher.finalize())[..16].to_string() // First 16 chars
-    }
-    
-    fn minimize_signature(&self, sig: &str) -> String {
-        // Remove whitespace, normalize types
-        sig.chars()
-            .filter(|c| !c.is_whitespace())
-            .collect::<String>()
-            .replace("&str", "s")
-            .replace("String", "S")
-            .replace("usize", "u")
-            .replace("i32", "i")
-    }
-    
-    pub fn get_by_hash(&self, hash: &str) -> Option<&CanonicalDecl> {
-        self.cache.get(hash)
-    }
-    
-    pub fn save_cache(&self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let json = serde_json::to_string_pretty(&self.cache)?;
-        std::fs::write(path, json)?;
-        Ok(())
-    }
-    
-    pub fn load_cache(&mut self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        if std::path::Path::new(path).exists() {
-            let json = std::fs::read_to_string(path)?;
-            self.cache = serde_json::from_str(&json)?;
-        }
-        Ok(())
-    }
-}
-
-// Integration with mklang! macro
-#[macro_export]
-macro_rules! canonical_hash {
-    (enum $name:ident { $($variant:ident),* }) => {
-        {
-            let mut canonicalizer = DeclCanonicalizer::new();
-            let variants = vec![$(stringify!($variant)),*];
-            canonicalizer.canonicalize_enum(stringify!($name), &variants).hash
-        }
-    };
-    
-    (struct $name:ident { $($field:ident),* }) => {
-        {
-            let mut canonicalizer = DeclCanonicalizer::new();
-            let fields = vec![$(stringify!($field)),*];
-            canonicalizer.canonicalize_struct(stringify!($name), &fields).hash
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/data_driven_compilation.rs b/compiler/introspector-collector/src/data_driven_compilation.rs
deleted file mode 100644
index 4746f47cc79..00000000000
--- a/compiler/introspector-collector/src/data_driven_compilation.rs
+++ /dev/null
@@ -1,22 +0,0 @@
-// Example usage of compile-time data-driven macros
-use introspector_macros::*;
-
-// Generate optimized implementations based on actual usage data
-usage_optimize!("usage_data/introspector_collector_usage.json");
-
-// Generate eigenform implementations based on mathematical analysis
-eigen_derive!("eigendata/rust_eigenvalues.json");
-
-// Generate hot path optimizations based on frequency data
-hot_path_optimize!("usage_data/frequency_analysis.json");
-
-#[cfg(test)]
-mod tests {
-    use super::*;
-    
-    #[test]
-    fn test_data_driven_compilation() {
-        // Test that our macros generated the right code based on data
-        println!("Compile-time data integration working!");
-    }
-}
diff --git a/compiler/introspector-collector/src/dwim_macro.rs b/compiler/introspector-collector/src/dwim_macro.rs
deleted file mode 100644
index 4c2f40bcb01..00000000000
--- a/compiler/introspector-collector/src/dwim_macro.rs
+++ /dev/null
@@ -1,64 +0,0 @@
-/// DWIM! - Do What I Mean macro (simplified)
-#[macro_export]
-macro_rules! dwim {
-    // Context: visiting expressions - usage-optimized hot paths
-    (visit_expr, $self:expr, $expr:expr) => {
-        match &$expr.kind {
-            // Hot path: literals (90% of expressions)
-            rustc_hir::ExprKind::Lit(_) => {
-                $self.collector.add_usage("literals", "literal_value".to_string(), 
-                    "literal_usage".to_string(), "Literal".to_string(), "Expr".to_string(),
-                    $self.crate_name.clone(), format!("{:?}", $expr.hir_id), None, None, None, None);
-            },
-            // Medium path: calls (8% of expressions)  
-            rustc_hir::ExprKind::Call(_, args) => {
-                $self.collector.add_usage("function_calls", format!("call_{}_args", args.len()),
-                    "function_call".to_string(), "Call".to_string(), "Expr".to_string(),
-                    $self.crate_name.clone(), "call_site".to_string(), None, None, None, None);
-            },
-            // Cold path: everything else (2% of expressions)
-            _ => {
-                $self.collector.add_usage("expressions", "other_expr".to_string(), "expr_usage".to_string(), 
-                    "Expression".to_string(), "Expr".to_string(), $self.crate_name.clone(),
-                    format!("{:?}", $expr.hir_id), None, None, None, None);
-            }
-        }
-    };
-    
-    // Context: visiting items - usage-optimized hot paths
-    (visit_item, $self:expr, $item:expr) => {
-        match &$item.kind {
-            // Hot: functions (60% of items)
-            rustc_hir::ItemKind::Fn { .. } => {
-                $self.collector.add_usage("functions", "function_name".to_string(),
-                    "function_decl".to_string(), "Function".to_string(), "Item".to_string(),
-                    $self.crate_name.clone(), format!("{:?}", $item.owner_id), None, None, None, None);
-            },
-            // Medium: structs (25% of items)
-            rustc_hir::ItemKind::Struct(..) => {
-                $self.collector.add_usage("structs", "struct_name".to_string(),
-                    "struct_decl".to_string(), "Struct".to_string(), "Item".to_string(),
-                    $self.crate_name.clone(), format!("{:?}", $item.owner_id), None, None, None, None);
-            },
-            // Cold: everything else (15% of items)
-            _ => {
-                $self.collector.add_usage("items", "other_item".to_string(),
-                    "item_decl".to_string(), "Item".to_string(), "Item".to_string(),
-                    $self.crate_name.clone(), format!("{:?}", $item.owner_id), None, None, None, None);
-            }
-        }
-    };
-    
-    // Context: type analysis - returns optimized usage type
-    (analyze_type, $tcx:expr, $def_id:expr) => {
-        {
-            let def_kind = $tcx.def_kind($def_id);
-            match def_kind {
-                rustc_hir::def::DefKind::AssocFn => "method_call".to_string(),
-                rustc_hir::def::DefKind::Fn => "function_call".to_string(),
-                rustc_hir::def::DefKind::Struct => "struct_usage".to_string(),
-                _ => "other_usage".to_string()
-            }
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/enum_macro_generator.rs b/compiler/introspector-collector/src/enum_macro_generator.rs
deleted file mode 100644
index 6686aad5e5b..00000000000
--- a/compiler/introspector-collector/src/enum_macro_generator.rs
+++ /dev/null
@@ -1,201 +0,0 @@
-use syn::{visit::Visit, Item, ItemEnum, Variant, File};
-use quote::quote;
-use std::collections::HashMap;
-
-pub struct EnumMacroGenerator {
-    pub enum_macros: HashMap<String, proc_macro2::TokenStream>,
-    pub usage_data: HashMap<String, u64>,
-}
-
-impl EnumMacroGenerator {
-    pub fn new() -> Self {
-        Self {
-            enum_macros: HashMap::new(),
-            usage_data: HashMap::new(),
-        }
-    }
-    
-    pub fn generate_from_source(&mut self, source_code: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let syntax_tree = syn::parse_file(source_code)?;
-        self.visit_file(&syntax_tree);
-        Ok(())
-    }
-    
-    pub fn generate_from_rustc_decls(&mut self) {
-        // Generate macros for common rustc enums
-        self.generate_item_kind_macro();
-        self.generate_expr_kind_macro();
-        self.generate_def_kind_macro();
-        self.generate_ty_kind_macro();
-    }
-    
-    fn generate_item_kind_macro(&mut self) {
-        let macro_code = quote! {
-            #[macro_export]
-            macro_rules! switch_item_kind {
-                ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-                    {
-                        // Auto-generated optimized switch for ItemKind
-                        // Ordered by frequency: Fn > Struct > Enum > Const > Static > Other
-                        match $expr {
-                            // Hot paths first (based on usage data)
-                            rustc_hir::ItemKind::Fn { .. } => {
-                                match ($($pattern => $result,)*) {
-                                    rustc_hir::ItemKind::Fn { .. } => $result,
-                                    _ => unreachable!()
-                                }
-                            },
-                            rustc_hir::ItemKind::Struct(..) => {
-                                match ($($pattern => $result,)*) {
-                                    rustc_hir::ItemKind::Struct(..) => $result,
-                                    _ => unreachable!()
-                                }
-                            },
-                            // Continue with other variants...
-                            $($pattern => $result,)*
-                        }
-                    }
-                };
-            }
-        };
-        
-        self.enum_macros.insert("ItemKind".to_string(), macro_code);
-    }
-    
-    fn generate_expr_kind_macro(&mut self) {
-        let macro_code = quote! {
-            #[macro_export]
-            macro_rules! switch_expr_kind {
-                ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-                    {
-                        // Auto-generated optimized switch for ExprKind
-                        // Ordered by frequency: Lit > Path > Call > Match > Field
-                        match $expr {
-                            rustc_hir::ExprKind::Lit(_) => {
-                                // Hot path - literals are very common
-                                match ($($pattern => $result,)*) {
-                                    rustc_hir::ExprKind::Lit(_) => $result,
-                                    _ => unreachable!()
-                                }
-                            },
-                            $($pattern => $result,)*
-                        }
-                    }
-                };
-            }
-        };
-        
-        self.enum_macros.insert("ExprKind".to_string(), macro_code);
-    }
-    
-    fn generate_def_kind_macro(&mut self) {
-        let macro_code = quote! {
-            #[macro_export]
-            macro_rules! switch_def_kind {
-                ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-                    {
-                        // Auto-generated optimized switch for DefKind
-                        // Jump table for high-frequency enum
-                        const DEF_KIND_JUMP_TABLE: &[fn() -> &'static str] = &[
-                            || "function_call",      // Fn - most common
-                            || "method_call",        // AssocFn - very common  
-                            || "struct_usage",       // Struct - common
-                            || "enum_variant_usage", // Variant - common
-                            || "enum_usage",         // Enum - medium
-                            || "const_usage",        // Const - medium
-                            || "static_usage",       // Static - low
-                            || "unknown_usage",      // Other - fallback
-                        ];
-                        
-                        let index = match $expr {
-                            rustc_hir::def::DefKind::Fn => 0,
-                            rustc_hir::def::DefKind::AssocFn => 1,
-                            rustc_hir::def::DefKind::Struct => 2,
-                            rustc_hir::def::DefKind::Variant => 3,
-                            rustc_hir::def::DefKind::Enum => 4,
-                            rustc_hir::def::DefKind::Const => 5,
-                            rustc_hir::def::DefKind::Static { .. } => 6,
-                            _ => 7,
-                        };
-                        
-                        DEF_KIND_JUMP_TABLE[index]()
-                    }
-                };
-            }
-        };
-        
-        self.enum_macros.insert("DefKind".to_string(), macro_code);
-    }
-    
-    fn generate_ty_kind_macro(&mut self) {
-        let macro_code = quote! {
-            #[macro_export]
-            macro_rules! switch_ty_kind {
-                ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-                    {
-                        // Auto-generated optimized switch for TyKind
-                        match $expr {
-                            $($pattern => $result,)*
-                        }
-                    }
-                };
-            }
-        };
-        
-        self.enum_macros.insert("TyKind".to_string(), macro_code);
-    }
-    
-    pub fn save_generated_macros(&self, output_path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let mut code = String::new();
-        
-        code.push_str("// Auto-generated enum switch macros\n");
-        code.push_str("// One macro per enum type, optimized by usage frequency\n\n");
-        
-        for (enum_name, macro_code) in &self.enum_macros {
-            code.push_str(&format!("// Optimized switch for {}\n", enum_name));
-            code.push_str(&macro_code.to_string());
-            code.push_str("\n\n");
-        }
-        
-        std::fs::write(output_path, code)?;
-        println!("Generated {} enum switch macros saved to: {}", self.enum_macros.len(), output_path);
-        Ok(())
-    }
-}
-
-impl<'ast> Visit<'ast> for EnumMacroGenerator {
-    fn visit_item_enum(&mut self, node: &'ast ItemEnum) {
-        let enum_name = node.ident.to_string();
-        println!("Generating switch macro for enum: {}", enum_name);
-        
-        let variants: Vec<_> = node.variants.iter()
-            .map(|v| &v.ident)
-            .collect();
-        
-        let macro_name = format!("switch_{}", enum_name.to_lowercase());
-        let macro_code = self.generate_enum_switch_macro(&enum_name, &macro_name, &variants);
-        
-        self.enum_macros.insert(enum_name, macro_code);
-    }
-}
-
-impl EnumMacroGenerator {
-    fn generate_enum_switch_macro(&self, enum_name: &str, macro_name: &str, variants: &[&syn::Ident]) -> proc_macro2::TokenStream {
-        let macro_ident = syn::Ident::new(macro_name, proc_macro2::Span::call_site());
-        
-        quote! {
-            #[macro_export]
-            macro_rules! #macro_ident {
-                ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-                    {
-                        // Auto-generated optimized switch for #enum_name
-                        // TODO: Order variants by usage frequency
-                        match $expr {
-                            $($pattern => $result,)*
-                        }
-                    }
-                };
-            }
-        }
-    }
-}
diff --git a/compiler/introspector-collector/src/generated/collection_helpers.rs b/compiler/introspector-collector/src/generated/collection_helpers.rs
deleted file mode 100644
index 0f4dc41b1c1..00000000000
--- a/compiler/introspector-collector/src/generated/collection_helpers.rs
+++ /dev/null
@@ -1,19 +0,0 @@
-// Auto-generated by mklang! macro - Structured collection helpers
-
-/// Canonical declaration helper
-#[macro_export]
-macro_rules! canonical_decl {
-    ($item:expr) => {{
-        let minimal_form = format!("{:?}", $item)
-            .chars()
-            .filter(|c| c.is_alphanumeric() || *c == '_')
-            .collect::<String>();
-        
-        use std::hash::{Hash, Hasher};
-        let mut hasher = std::collections::hash_map::DefaultHasher::new();
-        minimal_form.hash(&mut hasher);
-        let hash = hasher.finish();
-        
-        (minimal_form, format!("{:x}", hash))
-    }};
-}
diff --git a/compiler/introspector-collector/src/generated/eigenforms.rs b/compiler/introspector-collector/src/generated/eigenforms.rs
deleted file mode 100644
index b697a740f7a..00000000000
--- a/compiler/introspector-collector/src/generated/eigenforms.rs
+++ /dev/null
@@ -1,69 +0,0 @@
-// Auto-generated by mklang! macro - Eigenform analysis
-
-/// Mathematical eigenform for Rust structures
-pub struct RustEigenform {
-    pub canonical_form: String,
-    pub content_hash: String,
-    pub usage_frequency: u64,
-    pub eigenvalue: f64,
-    pub complexity_measure: f64,
-}
-
-impl RustEigenform {
-    pub fn new(canonical: String, hash: String) -> Self {
-        Self {
-            canonical_form: canonical,
-            content_hash: hash,
-            usage_frequency: 0,
-            eigenvalue: 1.0,
-            complexity_measure: 0.0,
-        }
-    }
-    
-    pub fn update_frequency(&mut self, count: u64) {
-        self.usage_frequency = count;
-        self.eigenvalue = (count as f64).ln().max(1.0);
-        self.complexity_measure = self.canonical_form.len() as f64 / self.eigenvalue;
-    }
-}
-
-/// Eigendecomposition analysis
-#[macro_export]
-macro_rules! analyze_eigenforms {
-    ($data:expr) => {{
-        let mut eigenforms = std::collections::HashMap::new();
-        
-        for entry in $data {
-            let (canonical, hash) = canonical_decl!(entry);
-            let eigenform = eigenforms.entry(hash.clone())
-                .or_insert_with(|| RustEigenform::new(canonical, hash));
-            eigenform.update_frequency(eigenform.usage_frequency + 1);
-        }
-        
-        eigenforms
-    }};
-}
-
-/// Content-addressable storage
-#[macro_export]
-macro_rules! store_content_addressed {
-    ($content:expr, $storage_path:expr) => {{
-        let (canonical, hash) = canonical_decl!($content);
-        let file_path = format!("{}/{}.json", $storage_path, hash);
-        
-        let data = serde_json::json!({
-            "canonical_form": canonical,
-            "content_hash": hash,
-            "content": $content,
-            "timestamp": std::time::SystemTime::now()
-                .duration_since(std::time::UNIX_EPOCH)
-                .unwrap()
-                .as_secs()
-        });
-        
-        std::fs::write(&file_path, serde_json::to_string_pretty(&data).unwrap())
-            .expect("Failed to write content-addressed file");
-        
-        hash
-    }};
-}
diff --git a/compiler/introspector-collector/src/generated/enum_switches.rs b/compiler/introspector-collector/src/generated/enum_switches.rs
deleted file mode 100644
index 711da1b89e9..00000000000
--- a/compiler/introspector-collector/src/generated/enum_switches.rs
+++ /dev/null
@@ -1,134 +0,0 @@
-
-// Auto-generated by syn-based macro generator
-
-#[macro_export]
-macro_rules! switch_itemkind2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_hir::ItemKind::Fn { .. } => "Fn".to_string(),
-            rustc_hir::ItemKind::Struct(..) => "Struct".to_string(),
-            rustc_hir::ItemKind::Enum(..) => "Enum".to_string(),
-            rustc_hir::ItemKind::Const(..) => "Const".to_string(),
-            rustc_hir::ItemKind::Static(..) => "Static".to_string(),
-            _ => "Other".to_string()
-        }
-    };
-    ($expr:expr) => {
-        match $expr {
-            rustc_hir::ItemKind::Fn { .. } => "Fn".to_string(),
-            rustc_hir::ItemKind::Struct(..) => "Struct".to_string(),
-            rustc_hir::ItemKind::Enum(..) => "Enum".to_string(),
-            rustc_hir::ItemKind::Const(..) => "Const".to_string(),
-            rustc_hir::ItemKind::Static(..) => "Static".to_string(),
-            _ => "Other".to_string()
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_defkind2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_hir::def::DefKind::Fn => "Fn".to_string(),
-            rustc_hir::def::DefKind::AssocFn => "AssocFn".to_string(),
-            rustc_hir::def::DefKind::Struct => "Struct".to_string(),
-            rustc_hir::def::DefKind::Variant => "Variant".to_string(),
-            _ => "Other".to_string()
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_mutability2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_hir::Mutability::Mut => "Mut".to_string(),
-            rustc_hir::Mutability::Not => "Not".to_string(),
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_node2string {
-    ($expr:expr) => {
-        match $expr {
-            rustc_hir::Node::Item(_) => "Item".to_string(),
-            rustc_hir::Node::Expr(_) => "Expr".to_string(),
-            rustc_hir::Node::Stmt(_) => "Stmt".to_string(),
-            rustc_hir::Node::TraitItem(_) => "TraitItem".to_string(),
-            rustc_hir::Node::ImplItem(_) => "ImplItem".to_string(),
-            _ => "Other".to_string()
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_litkind2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => $custom_result,)*
-            rustc_ast::LitKind::Str(..) => ("String".to_string(), "str_"),
-            rustc_ast::LitKind::Int(..) => ("Int".to_string(), "int_"),
-            rustc_ast::LitKind::Bool(..) => ("Bool".to_string(), "bool_"),
-            rustc_ast::LitKind::Float(..) => ("Float".to_string(), "float_"),
-            rustc_ast::LitKind::Char(..) => ("Char".to_string(), "char_"),
-            _ => ("Other".to_string(), "other_")
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_litinttype2string {
-    ($expr:expr, { $($pattern:pat => $custom:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $custom,)*
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::Isize) => "isize_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I8) => "i8_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I16) => "i16_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I32) => "i32_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I64) => "i64_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I128) => "i128_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::Usize) => "usize_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U8) => "u8_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U16) => "u16_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U32) => "u32_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U64) => "u64_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U128) => "u128_",
-            rustc_ast::LitIntType::Unsuffixed => "unsuffixed_",
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_litfloattype2string {
-    ($expr:expr, { $($pattern:pat => $custom:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $custom,)*
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F32) => "f32_",
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F64) => "f64_",
-            rustc_ast::LitFloatType::Unsuffixed => "float_unsuffixed_",
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_itemkind_custom {
-    ($expr:expr, { $($pattern:pat => $body:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $body,)*
-            _ => switch_itemkind2string!($expr)
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! switch_itemkind {
-    ($expr:expr => { $($pattern:pat => $body:block)* }) => {
-        match $expr {
-            $($pattern => $body)*
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/lib.rs b/compiler/introspector-collector/src/lib.rs
index 2958ba2446d..d9f32d39ef4 100644
--- a/compiler/introspector-collector/src/lib.rs
+++ b/compiler/introspector-collector/src/lib.rs
@@ -11,41 +11,6 @@
 pub mod visitors;
 pub mod usage_collector;
 pub mod collectors;
-pub mod self_mapper;
-pub mod typespace_mapper;
-pub mod rustmap_macros;
-pub mod metaprogram_generator;
-pub mod stylish_switches;
-pub mod enum_macro_generator;
-pub mod mklang_macro;
-pub mod canonical_decl;
-pub mod rust_enum_generator;
-pub mod structured_collection;
-pub mod schema_generator;
-pub mod proof_program;
-pub mod macro_documentation_generator;
-pub mod syn_macro_generator;
-pub mod dwim_macro;
-pub mod switch_bootstrap;
-
-// Auto-generated modules
-pub mod generated {
-    pub mod enum_switches;
-    pub mod collection_helpers;
-    pub mod eigenforms;
-    
-    // Include syn-generated macros if they exist
-    #[cfg(feature = "syn-generated")]
-    include!("generated/syn_enum_switches.rs");
-}
 
 //pub use usage_collector::UsageCollector;
 pub use data_structures::*;
-pub use stylish_switches::*;
-pub use mklang_macro::*;
-pub use structured_collection::*;
-pub use schema_generator::*;
-pub use generated::enum_switches::*;
-pub use rustmap_macros::*;
-pub use dwim_macro::*;
-pub use switch_bootstrap::*;
diff --git a/compiler/introspector-collector/src/macro_documentation_generator.rs b/compiler/introspector-collector/src/macro_documentation_generator.rs
deleted file mode 100644
index 65afed8e7bf..00000000000
--- a/compiler/introspector-collector/src/macro_documentation_generator.rs
+++ /dev/null
@@ -1,248 +0,0 @@
-use std::fs;
-use std::collections::HashMap;
-
-pub struct MacroDocumentationGenerator {
-    generated_macros: HashMap<String, MacroDoc>,
-    categories: HashMap<String, Vec<String>>,
-}
-
-#[derive(Debug, Clone)]
-pub struct MacroDoc {
-    pub name: String,
-    pub category: String,
-    pub description: String,
-    pub usage_example: String,
-    pub rust_type: String,
-    pub variants: Vec<String>,
-    pub content_hash: String,
-}
-
-impl MacroDocumentationGenerator {
-    pub fn new() -> Self {
-        Self {
-            generated_macros: HashMap::new(),
-            categories: HashMap::new(),
-        }
-    }
-    
-    pub fn add_macro_doc(&mut self, doc: MacroDoc) {
-        self.categories.entry(doc.category.clone())
-            .or_insert_with(Vec::new)
-            .push(doc.name.clone());
-        self.generated_macros.insert(doc.name.clone(), doc);
-    }
-    
-    pub fn generate_all_rust_enum_docs(&mut self) {
-        // HIR Enums
-        self.add_macro_doc(MacroDoc {
-            name: "switch_itemkind2string".to_string(),
-            category: "HIR Types".to_string(),
-            description: "Converts rustc_hir::ItemKind to optimized string representation".to_string(),
-            usage_example: r#"let item_type = switch_itemkind2string!(&item.kind, {
-    rustc_hir::ItemKind::Fn { .. } => "custom_fn",
-    rustc_hir::ItemKind::Struct(..) => "custom_struct",
-});"#.to_string(),
-            rust_type: "rustc_hir::ItemKind".to_string(),
-            variants: vec!["Fn".to_string(), "Struct".to_string(), "Enum".to_string(), "Const".to_string(), "Static".to_string()],
-            content_hash: "a1b2c3d4e5f6789a".to_string(),
-        });
-        
-        self.add_macro_doc(MacroDoc {
-            name: "switch_defkind2string".to_string(),
-            category: "HIR Types".to_string(),
-            description: "Converts rustc_hir::def::DefKind to optimized string representation".to_string(),
-            usage_example: r#"let def_usage = switch_defkind2string!(def_kind, {
-    rustc_hir::def::DefKind::Fn => "function_call",
-    rustc_hir::def::DefKind::Struct => "struct_usage",
-});"#.to_string(),
-            rust_type: "rustc_hir::def::DefKind".to_string(),
-            variants: vec!["Fn".to_string(), "AssocFn".to_string(), "Struct".to_string(), "Variant".to_string()],
-            content_hash: "b2c3d4e5f6789ab1".to_string(),
-        });
-        
-        self.add_macro_doc(MacroDoc {
-            name: "switch_exprkind2string".to_string(),
-            category: "HIR Types".to_string(),
-            description: "Converts rustc_hir::ExprKind to optimized string representation".to_string(),
-            usage_example: r#"let expr_type = switch_exprkind2string!(&expr.kind, {
-    rustc_hir::ExprKind::Lit(_) => "literal_value",
-    rustc_hir::ExprKind::Call(..) => "function_call",
-});"#.to_string(),
-            rust_type: "rustc_hir::ExprKind".to_string(),
-            variants: vec!["Lit".to_string(), "Call".to_string(), "Match".to_string()],
-            content_hash: "c3d4e5f6789ab1c2".to_string(),
-        });
-        
-        self.add_macro_doc(MacroDoc {
-            name: "switch_mutability2string".to_string(),
-            category: "HIR Types".to_string(),
-            description: "Converts rustc_hir::Mutability to optimized string representation".to_string(),
-            usage_example: r#"let ref_type = switch_mutability2string!(mutability, {
-    rustc_hir::Mutability::Mut => "mut_ref",
-    rustc_hir::Mutability::Not => "ref",
-});"#.to_string(),
-            rust_type: "rustc_hir::Mutability".to_string(),
-            variants: vec!["Mut".to_string(), "Not".to_string()],
-            content_hash: "f6789ab1c2d3e4f5".to_string(),
-        });
-        
-        // AST Enums
-        self.add_macro_doc(MacroDoc {
-            name: "switch_litkind2string".to_string(),
-            category: "AST Types".to_string(),
-            description: "Converts rustc_ast::LitKind to optimized string representation with type prefix".to_string(),
-            usage_example: r#"let (literal_value, type_prefix) = switch_litkind2string!(lit.node, {
-    rustc_ast::LitKind::Str(..) => ("custom_string", "str_"),
-    rustc_ast::LitKind::Int(..) => ("custom_int", "int_"),
-});"#.to_string(),
-            rust_type: "rustc_ast::LitKind".to_string(),
-            variants: vec!["Str".to_string(), "Int".to_string(), "Bool".to_string()],
-            content_hash: "d4e5f6789ab1c2d3".to_string(),
-        });
-        
-        self.add_macro_doc(MacroDoc {
-            name: "switch_litinttype2string".to_string(),
-            category: "AST Types".to_string(),
-            description: "Converts rustc_ast::LitIntType to optimized string representation".to_string(),
-            usage_example: r#"let int_type = switch_litinttype2string!(ty, {
-    rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I32) => "custom_i32",
-});"#.to_string(),
-            rust_type: "rustc_ast::LitIntType".to_string(),
-            variants: vec!["i8_".to_string(), "i16_".to_string(), "i32_".to_string(), "i64_".to_string(), "u8_".to_string(), "u16_".to_string(), "u32_".to_string(), "u64_".to_string()],
-            content_hash: "e5f6789ab1c2d3e4".to_string(),
-        });
-        
-        // Node Types
-        self.add_macro_doc(MacroDoc {
-            name: "switch_node2string".to_string(),
-            category: "Node Types".to_string(),
-            description: "Converts rustc_hir::Node to optimized string representation".to_string(),
-            usage_example: r#"let node_type = switch_node2string!(node);"#.to_string(),
-            rust_type: "rustc_hir::Node".to_string(),
-            variants: vec!["Item".to_string(), "Expr".to_string(), "Stmt".to_string(), "TraitItem".to_string(), "ImplItem".to_string()],
-            content_hash: "e5f6789ab1c2d3e4".to_string(),
-        });
-        
-        // Special Macros
-        self.add_macro_doc(MacroDoc {
-            name: "switch_itemkind_custom".to_string(),
-            category: "Special Macros".to_string(),
-            description: "Custom pattern matching for ItemKind with fallback to default switch".to_string(),
-            usage_example: r#"let result = switch_itemkind_custom!(&item.kind, {
-    rustc_hir::ItemKind::Const(_, _, _, body_id) => {
-        // Custom logic here
-        "custom_const".to_string()
-    },
-});"#.to_string(),
-            rust_type: "rustc_hir::ItemKind".to_string(),
-            variants: vec!["Custom patterns with fallback".to_string()],
-            content_hash: "custom".to_string(),
-        });
-        
-        self.add_macro_doc(MacroDoc {
-            name: "switch_itemkind".to_string(),
-            category: "Special Macros".to_string(),
-            description: "Block-based pattern matching for ItemKind".to_string(),
-            usage_example: r#"switch_itemkind!(&item.kind => {
-    rustc_hir::ItemKind::Const(_, _, _, body_id) => {
-        // Block of code here
-    }
-    _ => {}
-});"#.to_string(),
-            rust_type: "rustc_hir::ItemKind".to_string(),
-            variants: vec!["Block-based matching".to_string()],
-            content_hash: "block_based".to_string(),
-        });
-    }
-    
-    pub fn generate_markdown_documentation(&self) -> String {
-        let mut doc = String::new();
-        
-        doc.push_str("# Rust Eigendecomposition System - Generated Macros Documentation\n\n");
-        doc.push_str("This document describes all auto-generated macros for optimized Rust compiler introspection.\n\n");
-        
-        doc.push_str("## Overview\n\n");
-        doc.push_str("The `mklang!` macro system generates optimized switch macros for all Rust compiler enums, providing:\n\n");
-        doc.push_str("- ✅ **Content-addressable** canonical forms with SHA256 hashes\n");
-        doc.push_str("- ✅ **Usage-frequency** optimized pattern matching\n");
-        doc.push_str("- ✅ **Custom pattern** support with fallback defaults\n");
-        doc.push_str("- ✅ **Type-safe** string conversion for all Rust enums\n\n");
-        
-        doc.push_str("## Generated by\n\n");
-        doc.push_str("```rust\n");
-        doc.push_str("// build.rs\n");
-        doc.push_str("mklang!(rust_eigen_system);\n");
-        doc.push_str("```\n\n");
-        
-        // Generate documentation for each category
-        for (category, macro_names) in &self.categories {
-            doc.push_str(&format!("## {}\n\n", category));
-            
-            for macro_name in macro_names {
-                if let Some(macro_doc) = self.generated_macros.get(macro_name) {
-                    doc.push_str(&format!("### `{}`\n\n", macro_doc.name));
-                    doc.push_str(&format!("**Rust Type:** `{}`\n\n", macro_doc.rust_type));
-                    doc.push_str(&format!("**Content Hash:** `{}`\n\n", macro_doc.content_hash));
-                    doc.push_str(&format!("**Description:** {}\n\n", macro_doc.description));
-                    
-                    doc.push_str("**Supported Variants:**\n");
-                    for variant in &macro_doc.variants {
-                        doc.push_str(&format!("- `{}`\n", variant));
-                    }
-                    doc.push_str("\n");
-                    
-                    doc.push_str("**Usage Example:**\n");
-                    doc.push_str("```rust\n");
-                    doc.push_str(&macro_doc.usage_example);
-                    doc.push_str("\n```\n\n");
-                    
-                    doc.push_str("---\n\n");
-                }
-            }
-        }
-        
-        doc.push_str("## Macro Generator\n\n");
-        doc.push_str("All macros are generated using the `mkstring_switch_rust_hir!` meta-macro:\n\n");
-        doc.push_str("```rust\n");
-        doc.push_str("mkstring_switch_rust_hir!(ExprKind, switch_exprkind2string, {\n");
-        doc.push_str("    rustc_hir::ExprKind::Lit(_) => \"Lit\",\n");
-        doc.push_str("    rustc_hir::ExprKind::Call(..) => \"Call\",\n");
-        doc.push_str("    rustc_hir::ExprKind::Match(..) => \"Match\"\n");
-        doc.push_str("}, default: \"Other\");\n");
-        doc.push_str("```\n\n");
-        
-        doc.push_str("## Mathematical Foundation\n\n");
-        doc.push_str("Each macro represents a **categorical functor** from Rust's type system to optimized string representations:\n\n");
-        doc.push_str("```\n");
-        doc.push_str("Rust Enum → Canonical Form → Content Hash → Optimized Switch → String\n");
-        doc.push_str("```\n\n");
-        
-        doc.push_str("The system creates **eigenforms** that converge toward the mathematical essence of Rust through:\n");
-        doc.push_str("1. **Usage frequency analysis** - Hot paths get `#[likely]` annotations\n");
-        doc.push_str("2. **Content deduplication** - Identical patterns share canonical hashes\n");
-        doc.push_str("3. **Eigenvalue optimization** - Mathematical complexity measures guide generation\n\n");
-        
-        doc.push_str("## Integration\n\n");
-        doc.push_str("Import all generated macros:\n\n");
-        doc.push_str("```rust\n");
-        doc.push_str("use introspector_collector::*;\n");
-        doc.push_str("```\n\n");
-        doc.push_str("Or import specific macros:\n\n");
-        doc.push_str("```rust\n");
-        doc.push_str("use introspector_collector::{\n");
-        doc.push_str("    switch_itemkind2string,\n");
-        doc.push_str("    switch_defkind2string,\n");
-        doc.push_str("    switch_exprkind2string,\n");
-        doc.push_str("};\n");
-        doc.push_str("```\n\n");
-        
-        doc
-    }
-    
-    pub fn save_documentation(&self, path: &str) -> Result<(), std::io::Error> {
-        let doc = self.generate_markdown_documentation();
-        fs::write(path, doc)?;
-        println!("📚 Generated macro documentation: {}", path);
-        Ok(())
-    }
-}
diff --git a/compiler/introspector-collector/src/metaprogram_generator.rs b/compiler/introspector-collector/src/metaprogram_generator.rs
deleted file mode 100644
index 741f79b4688..00000000000
--- a/compiler/introspector-collector/src/metaprogram_generator.rs
+++ /dev/null
@@ -1,136 +0,0 @@
-use crate::rustmap_macros::*;
-use crate::{lift_structure, visitor_weave};
-use std::collections::HashMap;
-
-/// Eigenform wrapper for mathematical analysis
-#[derive(Debug, Clone)]
-pub struct EigenForm<T> {
-    pub value: T,
-    pub eigenvalues: Vec<f64>,
-    pub complexity: f64,
-}
-
-impl<T> EigenForm<T> {
-    pub fn new(value: T) -> Self {
-        Self {
-            value,
-            eigenvalues: vec![1.0], // Default eigenvalue
-            complexity: 1.0,
-        }
-    }
-}
-
-/// Our mathematical forms lifted from Rust
-lift_structure!(rustc_hir::Item => OurItem {
-    name: String,
-    kind: String,
-    complexity: f64
-});
-
-lift_structure!(rustc_hir::Expr => OurExpr {
-    kind: String,
-    usage_pattern: String,
-    eigenvalue: f64
-});
-
-lift_structure!(rustc_middle::ty::Ty => OurType {
-    name: String,
-    category: String,
-    dimension: usize
-});
-
-/// Visitor as projection/weave
-visitor_weave!(HIRProjection: rustc_hir::Item => OurItem);
-visitor_weave!(ExprWeave: rustc_hir::Expr => OurExpr);
-visitor_weave!(TypeWeave: rustc_middle::ty::Ty => OurType);
-
-/// Metaprogram generator for automatic lifting
-pub struct MetaprogramGenerator {
-    mappings: HashMap<String, String>,
-    projections: Vec<String>,
-}
-
-impl MetaprogramGenerator {
-    pub fn new() -> Self {
-        Self {
-            mappings: HashMap::new(),
-            projections: Vec::new(),
-        }
-    }
-    
-    pub fn generate_rustmaps(&mut self, typespace_mappings: &[crate::typespace_mapper::TypespaceMapping]) {
-        println!("=== GENERATING RUSTMAP! MACROS ===");
-        
-        for mapping in typespace_mappings {
-            if mapping.similarity_score > 0.8 {
-                // High similarity - generate duplicate mapping
-                self.generate_duplicate_map(mapping);
-            } else if mapping.similarity_score > 0.5 {
-                // Medium similarity - generate equivalent mapping with field lifting
-                self.generate_equivalent_map(mapping);
-            } else {
-                // Low similarity - novel type, generate projection
-                self.generate_projection_map(mapping);
-            }
-        }
-    }
-    
-    fn generate_duplicate_map(&mut self, mapping: &crate::typespace_mapper::TypespaceMapping) {
-        let rust_match = &mapping.rust_matches[0]; // Best match
-        
-        let macro_code = format!(
-            "rustmap!(duplicate {} => {});",
-            mapping.collector_element,
-            rust_match.rust_element
-        );
-        
-        println!("DUPLICATE: {}", macro_code);
-        self.mappings.insert(mapping.collector_element.clone(), macro_code);
-    }
-    
-    fn generate_equivalent_map(&mut self, mapping: &crate::typespace_mapper::TypespaceMapping) {
-        let rust_match = &mapping.rust_matches[0];
-        
-        let macro_code = format!(
-            "rustmap!(equivalent {} => {} {{ /* fields to be analyzed */ }});",
-            mapping.collector_element,
-            rust_match.rust_element
-        );
-        
-        println!("EQUIVALENT: {}", macro_code);
-        self.mappings.insert(mapping.collector_element.clone(), macro_code);
-    }
-    
-    fn generate_projection_map(&mut self, mapping: &crate::typespace_mapper::TypespaceMapping) {
-        let projection_code = format!(
-            "visitor_weave!({}Projection: RustType => {});",
-            mapping.collector_element,
-            mapping.collector_element
-        );
-        
-        println!("PROJECTION: {}", projection_code);
-        self.projections.push(projection_code);
-    }
-    
-    pub fn save_generated_code(&self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let mut code = String::new();
-        
-        code.push_str("// Auto-generated rustmap! macros\n");
-        code.push_str("use crate::rustmap_macros::*;\n\n");
-        
-        for (_, mapping_code) in &self.mappings {
-            code.push_str(&mapping_code);
-            code.push_str("\n");
-        }
-        
-        code.push_str("\n// Auto-generated projections/weaves\n");
-        for projection in &self.projections {
-            code.push_str(&projection);
-            code.push_str("\n");
-        }
-        
-        std::fs::write(path, code)?;
-        println!("Generated metaprogram saved to: {}", path);
-        Ok(())
-    }
-}
diff --git a/compiler/introspector-collector/src/mklang_macro.rs b/compiler/introspector-collector/src/mklang_macro.rs
deleted file mode 100644
index 8f90cb48d36..00000000000
--- a/compiler/introspector-collector/src/mklang_macro.rs
+++ /dev/null
@@ -1,451 +0,0 @@
-/// The ultimate meta-macro that generates the entire introspection system
-#[macro_export]
-macro_rules! mklang {
-    // Full system generation
-    (rust_eigen_system) => {
-        mklang!(
-            collect_usage: true,
-            generate_switches: true,
-            optimize_matches: true,
-            create_projections: true,
-            build_eigenforms: true
-        );
-    };
-    
-    // Parameterized system generation
-    (
-        collect_usage: $collect:expr,
-        generate_switches: $switches:expr,
-        optimize_matches: $optimize:expr,
-        create_projections: $project:expr,
-        build_eigenforms: $eigen:expr
-    ) => {
-        {
-            use std::env;
-            use std::path::Path;
-            
-            println!("cargo:rerun-if-changed=src/");
-            println!("cargo:rerun-if-changed=usage_data/");
-            
-            // Step 1: Collect usage data if enabled
-            if $collect {
-                mklang!(@collect_usage_data);
-            }
-            
-            // Step 2: Generate enum switches if enabled  
-            if $switches {
-                mklang!(@generate_enum_switches);
-            }
-            
-            // Step 3: Optimize match statements if enabled
-            if $optimize {
-                mklang!(@optimize_matches);
-            }
-            
-            // Step 4: Create type projections if enabled
-            if $project {
-                mklang!(@create_projections);
-            }
-            
-            // Step 5: Build eigenforms if enabled
-            if $eigen {
-                mklang!(@build_eigenforms);
-            }
-            
-            // Step 6: Generate documentation
-            mklang!(@generate_documentation);
-            
-            println!("=== MKLANG! SYSTEM GENERATED ===");
-        }
-    };
-    
-    // Internal: Collect usage data
-    (@collect_usage_data) => {
-        {
-            println!("Collecting usage data...");
-            
-            // Try multiple data sources in order of preference
-            let data = mklang!(@load_data_sources);
-            
-            if data.is_empty() {
-                // Fallback: generate fresh data
-                std::process::Command::new("cargo")
-                    .args(&["run", "--bin", "working_usage_collector"])
-                    .status()
-                    .expect("Failed to generate fresh usage data");
-            }
-        }
-    };
-    
-    // Internal: Load data from multiple sources
-    (@load_data_sources) => {
-        {
-            use std::env;
-            use crate::canonical_decl::DeclCanonicalizer;
-            
-            let mut canonicalizer = DeclCanonicalizer::new();
-            canonicalizer.load_cache(".cache/canonical_decls.json").ok();
-            
-            // Source 1: Check IPFS by content hash
-            if let Ok(content_hash) = env::var("RUST_DECL_HASH") {
-                println!("Loading canonical decl by hash: {}", content_hash);
-                if let Some(decl) = canonicalizer.get_by_hash(&content_hash) {
-                    return decl.minimal_form.clone();
-                }
-                
-                // Try IPFS lookup
-                if let Ok(data) = mklang!(@load_from_ipfs &content_hash) {
-                    return data;
-                }
-            }
-            
-            // Source 2: Check Nix store by hash
-            if let Ok(nix_hash) = env::var("RUST_USAGE_NIX_HASH") {
-                let nix_path = format!("/nix/store/{}-rust-decls", nix_hash);
-                println!("Loading from Nix store: {}", nix_path);
-                if let Ok(data) = std::fs::read_to_string(&nix_path) {
-                    return data;
-                }
-            }
-            
-            // Source 3: Check local cache by hash
-            let cache_paths = [
-                "usage_data/cached_rust_usage.json",
-                ".cache/rust_eigendata.json", 
-                "/tmp/rust_usage_cache.json"
-            ];
-            
-            for cache_path in &cache_paths {
-                if std::path::Path::new(cache_path).exists() {
-                    println!("Loading from cache: {}", cache_path);
-                    if let Ok(data) = std::fs::read_to_string(cache_path) {
-                        return data;
-                    }
-                }
-            }
-            
-            String::new() // No data found
-        }
-    };
-    
-    // Internal: Load from IPFS
-    (@load_from_ipfs $hash:expr) => {
-        {
-            std::process::Command::new("ipfs")
-                .args(&["cat", $hash])
-                .output()
-                .map(|output| String::from_utf8_lossy(&output.stdout).to_string())
-                .map_err(|e| format!("IPFS error: {}", e))
-        }
-    };
-    
-    // Internal: Generate enum switches
-    (@generate_enum_switches) => {
-        {
-            println!("Generating enum switch macros using syn...");
-            
-            use crate::syn_macro_generator::SynMacroGenerator;
-            let mut generator = SynMacroGenerator::new();
-            
-            // Generate macros for all Rust enums using syn
-            generator.generate_all_rust_enum_macros();
-            
-            // Save to generated directory
-            std::fs::create_dir_all("src/generated").unwrap();
-            generator.save_generated_macros("src/generated/syn_enum_switches.rs").unwrap();
-            
-            println!("✅ Syn-generated enum switches saved to src/generated/syn_enum_switches.rs");
-        }
-    };
-    
-    // Internal: Generate include file for all enum switches
-    (@generate_enum_includes) => {
-        {
-            format!(r#"
-// Auto-generated by mklang! - All Rust enum switch macros
-// Content-addressable and optimized by usage frequency
-
-// HIR enums
-include!("generated/enum_switches/switch_itemkind.rs");
-include!("generated/enum_switches/switch_exprkind.rs");
-include!("generated/enum_switches/switch_patkind.rs");
-include!("generated/enum_switches/switch_tykind.rs");
-
-// AST enums  
-include!("generated/enum_switches/switch_litkind.rs");
-include!("generated/enum_switches/switch_binopkind.rs");
-include!("generated/enum_switches/switch_unopkind.rs");
-
-// Middle enums
-include!("generated/enum_switches/switch_defkind.rs");
-
-// Def enums
-include!("generated/enum_switches/switch_mutability.rs");
-include!("generated/enum_switches/switch_safety.rs");
-include!("generated/enum_switches/switch_constness.rs");
-
-// Usage examples:
-// switch_itemkind!(item.kind => {{
-//     ItemKind::Fn {{ .. }} => "function",
-//     ItemKind::Struct(..) => "struct",
-//     _ => "other"
-// }})
-
-// switch_defkind!(def_kind => {{
-//     DefKind::Fn => "function_call",
-//     DefKind::Struct => "struct_usage", 
-//     _ => "unknown"
-// }})
-"#)
-        }
-    };
-    
-    // Internal: Generate switch code
-    (@enum_switches_code) => {
-        {
-            // Load frequency data from available sources
-            let frequency_data = mklang!(@load_frequency_data);
-            
-            format!(r#"
-// Auto-generated by mklang! macro using data from: {}
-use proc_macro2::TokenStream;
-use quote::quote;
-
-// ItemKind switch - optimized by frequency data
-#[macro_export]
-macro_rules! switch_item_kind {{
-    ($expr:expr => {{ $($pattern:pat => $result:expr),* $(,)? }}) => {{
-        {{
-            // Frequency-optimized order: {}
-            match $expr {{
-                // Hot paths first (>1000 occurrences)
-                rustc_hir::ItemKind::Fn {{ .. }} => {{
-                    $($($pattern => $result,)*)?
-                }},
-                // Medium paths (100-1000 occurrences)  
-                rustc_hir::ItemKind::Struct(..) => {{
-                    $($($pattern => $result,)*)?
-                }},
-                // Cold paths (<100 occurrences)
-                $($pattern => $result,)*
-            }}
-        }}
-    }};
-}}
-"#, 
-            frequency_data.source,
-            frequency_data.order_summary)
-        }
-    };
-    
-    // Internal: Load frequency data from sources
-    (@load_frequency_data) => {
-        {
-            #[derive(Default)]
-            struct FrequencyData {
-                source: String,
-                order_summary: String,
-                frequencies: std::collections::HashMap<String, u64>,
-            }
-            
-            let mut data = FrequencyData::default();
-            
-            // Try IPFS first
-            if let Ok(ipfs_hash) = std::env::var("RUST_FREQUENCY_IPFS") {
-                data.source = format!("IPFS:{}", ipfs_hash);
-                // Load and parse frequency data from IPFS
-            }
-            // Try Nix store
-            else if let Ok(nix_path) = std::env::var("RUST_FREQUENCY_NIX") {
-                data.source = format!("Nix:{}", nix_path);
-                // Load from Nix store
-            }
-            // Try local cache
-            else if std::path::Path::new("frequency_cache.json").exists() {
-                data.source = "Local cache".to_string();
-                // Load from local cache
-            }
-            // Fallback to defaults
-            else {
-                data.source = "Default heuristics".to_string();
-                data.order_summary = "Fn(3000) > Struct(1500) > Enum(800) > Const(200)".to_string();
-            }
-            
-            data
-        }
-    };
-    
-    // Internal: Optimize matches
-    (@optimize_matches) => {
-        {
-            println!("Optimizing match statements...");
-            
-            // Scan source files and optimize matches
-            let optimization_code = mklang!(@match_optimizations);
-            std::fs::write("src/generated_optimizations.rs", optimization_code).unwrap();
-        }
-    };
-    
-    // Internal: Generate match optimizations
-    (@match_optimizations) => {
-        {
-            format!(r#"
-// Auto-generated match optimizations
-use crate::data_structures::*;
-
-impl UsageCollector {{
-    // Optimized classification based on frequency data
-    pub fn classify_usage_optimized(&self, usage: &str, usage_type: &str) -> UsageClassification {{
-        let mut classification = UsageClassification::default();
-        
-        // Reordered by frequency: string_conversion is most common
-        if usage.contains("to_string") || usage.contains("Display") {{
-            classification.string_conversion = 1;
-        }} else if usage.contains("match") || usage.contains("if let") {{
-            classification.pattern_matching = 1;
-        }} else if usage.contains("::") && !usage.contains("(") {{
-            classification.construction = 1;
-        }} else {{
-            classification.general = 1;
-        }}
-        
-        classification
-    }}
-}}
-"#)
-        }
-    };
-    
-    // Internal: Create projections
-    (@create_projections) => {
-        {
-            println!("Creating type projections...");
-            
-            let projection_code = mklang!(@projection_code);
-            std::fs::write("src/generated_projections.rs", projection_code).unwrap();
-        }
-    };
-    
-    // Internal: Generate projection code
-    (@projection_code) => {
-        {
-            format!(r#"
-// Auto-generated type projections
-use crate::rustmap_macros::*;
-
-// Projections from Rust types to our mathematical forms
-visitor_weave!(ItemProjection: rustc_hir::Item => OurItem);
-visitor_weave!(ExprProjection: rustc_hir::Expr => OurExpr);
-visitor_weave!(TypeProjection: rustc_middle::ty::Ty => OurType);
-
-// Eigenform wrappers
-#[derive(Debug, Clone)]
-pub struct EigenItem {{
-    pub item: OurItem,
-    pub eigenvalue: f64,
-    pub complexity: f64,
-}}
-
-impl From<rustc_hir::Item> for EigenItem {{
-    fn from(item: rustc_hir::Item) -> Self {{
-        let our_item = ItemProjection::new(|i| OurItem::from(i)).weave(item);
-        Self {{
-            item: our_item,
-            eigenvalue: 1.0, // TODO: Calculate from usage data
-            complexity: 1.0,
-        }}
-    }}
-}}
-"#)
-        }
-    };
-    
-    // Internal: Build eigenforms
-    (@build_eigenforms) => {
-        {
-            println!("Building eigenforms and running proof program...");
-            
-            // Generate JSON schema that matches our collected structures
-            use crate::schema_generator::*;
-            save_schema!("src/generated_rust_schema.json");
-            
-            // Run the proof program
-            std::process::Command::new("cargo")
-                .args(&["run", "--bin", "proof_program"])
-                .status()
-                .expect("Failed to run proof program");
-            
-            let eigen_code = mklang!(@eigenform_code);
-            std::fs::write("src/generated_eigenforms.rs", eigen_code).unwrap();
-        }
-    };
-    
-    // Internal: Generate eigenform code
-    (@eigenform_code) => {
-        {
-            format!(r#"
-// Auto-generated eigenform system
-use std::collections::HashMap;
-
-#[derive(Debug, Clone)]
-pub struct RustEigenSystem {{
-    pub eigenvalues: HashMap<String, f64>,
-    pub eigenvectors: HashMap<String, Vec<f64>>,
-    pub complexity_matrix: Vec<Vec<f64>>,
-}}
-
-impl RustEigenSystem {{
-    pub fn new() -> Self {{
-        let mut eigenvalues = HashMap::new();
-        
-        // Computed eigenvalues from usage analysis
-        eigenvalues.insert("ItemKind::Fn".to_string(), 3.14159);
-        eigenvalues.insert("ItemKind::Struct".to_string(), 2.71828);
-        eigenvalues.insert("DefKind::Variant".to_string(), 1.61803);
-        
-        Self {{
-            eigenvalues,
-            eigenvectors: HashMap::new(),
-            complexity_matrix: vec![vec![1.0; 71]; 71], // 71x71 complexity matrix
-        }}
-    }}
-    
-    pub fn decompose(&self, rust_element: &str) -> f64 {{
-        self.eigenvalues.get(rust_element).copied().unwrap_or(1.0)
-    }}
-}}
-
-// The mathematical essence of Rust
-pub static RUST_EIGEN: RustEigenSystem = RustEigenSystem {{
-    eigenvalues: HashMap::new(), // Will be populated at runtime
-    eigenvectors: HashMap::new(),
-    complexity_matrix: vec![],
-}};
-"#)
-        }
-    };
-    
-    // Internal: Generate comprehensive documentation
-    (@generate_documentation) => {
-        {
-            println!("Generating comprehensive macro documentation...");
-            
-            use crate::macro_documentation_generator::MacroDocumentationGenerator;
-            let mut doc_generator = MacroDocumentationGenerator::new();
-            
-            // Generate docs for all generated macros
-            doc_generator.generate_all_rust_enum_docs();
-            
-            // Save documentation files
-            doc_generator.save_documentation("GENERATED_MACROS.md").unwrap();
-            doc_generator.save_documentation("docs/macro_reference.md").unwrap_or_else(|_| {
-                std::fs::create_dir_all("docs").unwrap();
-                doc_generator.save_documentation("docs/macro_reference.md").unwrap();
-            });
-            
-            println!("📚 Documentation generated:");
-            println!("  - GENERATED_MACROS.md");
-            println!("  - docs/macro_reference.md");
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/proof_program.rs b/compiler/introspector-collector/src/proof_program.rs
deleted file mode 100644
index 88b8f8f2d3e..00000000000
--- a/compiler/introspector-collector/src/proof_program.rs
+++ /dev/null
@@ -1,408 +0,0 @@
-/// The Proof Program: Self-analysis and improvement suggestions
-use crate::{canonical_decl::*, typespace_mapper::*, structured_collection::*};
-use std::collections::{HashMap, HashSet};
-use std::hash::Hasher;
-use serde::{Serialize, Deserialize};
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct ProofAnalysis {
-    pub duplicated_from_rust: Vec<DuplicateMatch>,
-    pub missing_from_rust: Vec<MissingElement>, 
-    pub truly_novel: Vec<NovelElement>,
-    pub improvement_suggestions: Vec<ImprovementSuggestion>,
-    pub rewrite_recommendations: Vec<RewriteRecommendation>,
-}
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct DuplicateMatch {
-    pub our_element: String,
-    pub rust_equivalent: String,
-    pub similarity: f64,
-    pub canonical_hash: String,
-    pub suggestion: String,
-}
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct MissingElement {
-    pub rust_element: String,
-    pub why_missing: String,
-    pub should_implement: bool,
-    pub implementation_suggestion: String,
-}
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct NovelElement {
-    pub our_element: String,
-    pub novelty_score: f64,
-    pub mathematical_significance: String,
-    pub contribution_to_eigenspace: String,
-}
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct ImprovementSuggestion {
-    pub file: String,
-    pub line: u32,
-    pub current_code: String,
-    pub suggested_code: String,
-    pub reason: String,
-    pub eigenvalue_improvement: f64,
-}
-
-#[derive(Debug, Serialize, Deserialize)]
-pub struct RewriteRecommendation {
-    pub module: String,
-    pub current_approach: String,
-    pub mathematical_approach: String,
-    pub rust_canonical_form: String,
-    pub rewritten_code: String,
-}
-
-pub struct ProofProgram {
-    our_codebase: HashMap<String, StructuredData>,
-    rust_model: HashMap<String, StructuredData>,
-    canonicalizer: DeclCanonicalizer,
-}
-
-impl ProofProgram {
-    pub fn new() -> Self {
-        Self {
-            our_codebase: HashMap::new(),
-            rust_model: HashMap::new(),
-            canonicalizer: DeclCanonicalizer::new(),
-        }
-    }
-    
-    /// Read and analyze our own codebase
-    pub fn read_self(&mut self) -> Result<(), Box<dyn std::error::Error>> {
-        println!("🔍 PROOF PROGRAM: Reading self...");
-        
-        // Read all our source files
-        let source_files = glob::glob("src/**/*.rs")?;
-        
-        for file_path in source_files.flatten() {
-            let content = std::fs::read_to_string(&file_path)?;
-            let syntax_tree = syn::parse_file(&content)?;
-            
-            // Extract all our structures
-            for item in syntax_tree.items {
-                let structured = self.analyze_our_item(&item);
-                let hash = structured.content_hash.clone();
-                self.our_codebase.insert(hash, structured);
-            }
-        }
-        
-        println!("📊 Analyzed {} elements from our codebase", self.our_codebase.len());
-        Ok(())
-    }
-    
-    /// Read experimental codebase
-    pub fn read_experiments(&mut self) -> Result<(), Box<dyn std::error::Error>> {
-        println!("🧪 PROOF PROGRAM: Reading experiments...");
-        
-        let experiment_files = glob::glob("../experimental-stuff/src/**/*.rs")?;
-        
-        for file_path in experiment_files.flatten() {
-            let content = std::fs::read_to_string(&file_path)?;
-            if let Ok(syntax_tree) = syn::parse_file(&content) {
-                for item in syntax_tree.items {
-                    let structured = self.analyze_our_item(&item);
-                    let hash = structured.content_hash.clone();
-                    self.our_codebase.insert(hash, structured);
-                }
-            }
-        }
-        
-        Ok(())
-    }
-    
-    /// Load Rust's canonical model
-    pub fn load_rust_model(&mut self) -> Result<(), Box<dyn std::error::Error>> {
-        println!("🦀 PROOF PROGRAM: Loading Rust model...");
-        
-        // Load from our collected usage data
-        if let Ok(data) = std::fs::read_to_string("usage_data/rust_canonical_model.json") {
-            self.rust_model = serde_json::from_str(&data)?;
-        } else {
-            // Generate from known Rust structures
-            self.generate_rust_model();
-        }
-        
-        println!("📊 Loaded {} elements from Rust model", self.rust_model.len());
-        Ok(())
-    }
-    
-    /// The core proof: analyze what's duplicated, missing, and novel
-    pub fn generate_proof(&self) -> ProofAnalysis {
-        println!("🎯 PROOF PROGRAM: Generating proof analysis...");
-        
-        let mut analysis = ProofAnalysis {
-            duplicated_from_rust: Vec::new(),
-            missing_from_rust: Vec::new(),
-            truly_novel: Vec::new(),
-            improvement_suggestions: Vec::new(),
-            rewrite_recommendations: Vec::new(),
-        };
-        
-        // Find duplicates
-        for (our_hash, our_element) in &self.our_codebase {
-            if let Some(rust_equivalent) = self.find_rust_equivalent(our_element) {
-                let similarity = self.calculate_similarity(our_element, rust_equivalent);
-                
-                if similarity > 0.9 {
-                    analysis.duplicated_from_rust.push(DuplicateMatch {
-                        our_element: our_element.type_name.clone(),
-                        rust_equivalent: rust_equivalent.type_name.clone(),
-                        similarity,
-                        canonical_hash: our_hash.clone(),
-                        suggestion: format!("Replace with rustc::{}", rust_equivalent.type_name),
-                    });
-                }
-            } else {
-                // Truly novel element
-                analysis.truly_novel.push(NovelElement {
-                    our_element: our_element.type_name.clone(),
-                    novelty_score: self.calculate_novelty(our_element),
-                    mathematical_significance: self.assess_mathematical_significance(our_element),
-                    contribution_to_eigenspace: self.assess_eigenspace_contribution(our_element),
-                });
-            }
-        }
-        
-        // Find missing elements
-        for (rust_hash, rust_element) in &self.rust_model {
-            if !self.our_codebase.contains_key(rust_hash) {
-                analysis.missing_from_rust.push(MissingElement {
-                    rust_element: rust_element.type_name.clone(),
-                    why_missing: self.analyze_why_missing(rust_element),
-                    should_implement: self.should_implement(rust_element),
-                    implementation_suggestion: self.suggest_implementation(rust_element),
-                });
-            }
-        }
-        
-        // Generate improvement suggestions
-        analysis.improvement_suggestions = self.generate_improvements();
-        analysis.rewrite_recommendations = self.generate_rewrites();
-        
-        analysis
-    }
-    
-    /// Generate line-by-line improvement suggestions
-    fn generate_improvements(&self) -> Vec<ImprovementSuggestion> {
-        let mut suggestions = Vec::new();
-        
-        // Analyze each file for improvements
-        for (hash, element) in &self.our_codebase {
-            if let Some(rust_equivalent) = self.find_rust_equivalent(element) {
-                let eigenvalue_improvement = self.calculate_eigenvalue_improvement(element, rust_equivalent);
-                
-                if eigenvalue_improvement > 0.1 {
-                    suggestions.push(ImprovementSuggestion {
-                        file: element.category.clone(),
-                        line: 1, // TODO: Extract actual line numbers
-                        current_code: element.canonical_form.clone(),
-                        suggested_code: rust_equivalent.canonical_form.clone(),
-                        reason: format!("Align with Rust canonical form for {}", rust_equivalent.type_name),
-                        eigenvalue_improvement,
-                    });
-                }
-            }
-        }
-        
-        suggestions
-    }
-    
-    /// Generate complete module rewrites
-    fn generate_rewrites(&self) -> Vec<RewriteRecommendation> {
-        let mut rewrites = Vec::new();
-        
-        // Group elements by module and suggest mathematical rewrites
-        let mut modules: HashMap<String, Vec<&StructuredData>> = HashMap::new();
-        for element in self.our_codebase.values() {
-            modules.entry(element.category.clone()).or_default().push(element);
-        }
-        
-        for (module, elements) in modules {
-            if elements.len() > 3 { // Only rewrite substantial modules
-                rewrites.push(RewriteRecommendation {
-                    module: module.clone(),
-                    current_approach: format!("{} separate elements", elements.len()),
-                    mathematical_approach: "Unified eigenform with categorical structure".to_string(),
-                    rust_canonical_form: self.generate_canonical_module_form(&elements),
-                    rewritten_code: self.generate_mathematical_rewrite(&module, &elements),
-                });
-            }
-        }
-        
-        rewrites
-    }
-    
-    pub fn save_proof(&self, analysis: &ProofAnalysis, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let json = serde_json::to_string_pretty(analysis)?;
-        std::fs::write(path, json)?;
-        
-        // Also generate human-readable report
-        let report = self.generate_human_report(analysis);
-        std::fs::write(path.replace(".json", "_report.md"), report)?;
-        
-        println!("💾 Proof analysis saved to: {}", path);
-        Ok(())
-    }
-    
-    fn generate_human_report(&self, analysis: &ProofAnalysis) -> String {
-        format!(r#"# Proof Analysis Report
-
-## Summary
-- **Duplicated from Rust**: {} elements
-- **Missing from Rust**: {} elements  
-- **Truly Novel**: {} elements
-- **Improvement Suggestions**: {}
-- **Rewrite Recommendations**: {}
-
-## Duplicated Elements
-{}
-
-## Novel Contributions
-{}
-
-## Missing Elements We Should Implement
-{}
-
-## Improvement Suggestions
-{}
-
-## Mathematical Rewrite Recommendations
-{}
-
----
-*Generated by the Proof Program - Self-analyzing eigendecomposition system*
-"#,
-            analysis.duplicated_from_rust.len(),
-            analysis.missing_from_rust.len(),
-            analysis.truly_novel.len(),
-            analysis.improvement_suggestions.len(),
-            analysis.rewrite_recommendations.len(),
-            self.format_duplicates(&analysis.duplicated_from_rust),
-            self.format_novel(&analysis.truly_novel),
-            self.format_missing(&analysis.missing_from_rust),
-            self.format_improvements(&analysis.improvement_suggestions),
-            self.format_rewrites(&analysis.rewrite_recommendations)
-        )
-    }
-    
-    // Helper methods (implemented for proof demonstration)
-    fn analyze_our_item(&self, item: &syn::Item) -> StructuredData { 
-        StructuredData {
-            category: "our_code".to_string(),
-            type_name: match item {
-                syn::Item::Struct(s) => format!("struct_{}", s.ident),
-                syn::Item::Enum(e) => format!("enum_{}", e.ident),
-                syn::Item::Fn(f) => format!("fn_{}", f.sig.ident),
-                _ => "other".to_string(),
-            },
-            canonical_form: "syn_item".to_string(),
-            content_hash: format!("{:x}", std::collections::hash_map::DefaultHasher::new().finish()),
-            fields: vec![],
-            variants: vec![],
-            usage_context: UsageContext {
-                module: "unknown".to_string(),
-                function: "unknown".to_string(),
-                line: 0,
-                frequency: 1,
-            },
-        }
-    }
-    
-    fn generate_rust_model(&mut self) { 
-        // Generate known Rust structures for comparison
-        let rust_item = StructuredData {
-            category: "rust_core".to_string(),
-            type_name: "ItemKind".to_string(),
-            canonical_form: "enum ItemKind{Const,Enum,Fn,Static,Struct}".to_string(),
-            content_hash: "a1b2c3d4e5f6789a".to_string(),
-            fields: vec![],
-            variants: vec!["Const".to_string(), "Enum".to_string(), "Fn".to_string()],
-            usage_context: UsageContext {
-                module: "rustc_hir".to_string(),
-                function: "compiler".to_string(),
-                line: 0,
-                frequency: 1000,
-            },
-        };
-        self.rust_model.insert(rust_item.content_hash.clone(), rust_item);
-    }
-    
-    fn find_rust_equivalent(&self, element: &StructuredData) -> Option<&StructuredData> { 
-        // Simple similarity matching
-        for rust_element in self.rust_model.values() {
-            if element.type_name.contains("enum") && rust_element.type_name.contains("Kind") {
-                return Some(rust_element);
-            }
-        }
-        None
-    }
-    
-    fn calculate_similarity(&self, a: &StructuredData, b: &StructuredData) -> f64 { 
-        if a.type_name == b.type_name { 1.0 } else { 0.5 }
-    }
-    
-    fn calculate_novelty(&self, element: &StructuredData) -> f64 { 
-        if element.type_name.contains("Proof") || element.type_name.contains("Eigen") { 0.9 } else { 0.3 }
-    }
-    
-    fn assess_mathematical_significance(&self, element: &StructuredData) -> String { 
-        if element.type_name.contains("Eigen") {
-            "High - contributes to eigendecomposition".to_string()
-        } else {
-            "Medium - standard programming construct".to_string()
-        }
-    }
-    
-    fn assess_eigenspace_contribution(&self, element: &StructuredData) -> String { 
-        format!("Extends eigenspace by introducing {}", element.type_name)
-    }
-    
-    fn analyze_why_missing(&self, element: &StructuredData) -> String { 
-        format!("Not implemented - {} is core Rust functionality", element.type_name)
-    }
-    
-    fn should_implement(&self, element: &StructuredData) -> bool { 
-        element.usage_context.frequency > 100
-    }
-    
-    fn suggest_implementation(&self, element: &StructuredData) -> String { 
-        format!("Consider implementing {} for completeness", element.type_name)
-    }
-    
-    fn calculate_eigenvalue_improvement(&self, a: &StructuredData, b: &StructuredData) -> f64 { 
-        (b.usage_context.frequency as f64 / a.usage_context.frequency.max(1) as f64).min(1.0)
-    }
-    
-    fn generate_canonical_module_form(&self, elements: &[&StructuredData]) -> String { 
-        format!("module_with_{}_elements", elements.len())
-    }
-    
-    fn generate_mathematical_rewrite(&self, module: &str, elements: &[&StructuredData]) -> String { 
-        format!("// Mathematical rewrite of {}\n// {} elements unified into eigenform", module, elements.len())
-    }
-    
-    fn format_duplicates(&self, duplicates: &[DuplicateMatch]) -> String { 
-        duplicates.iter().map(|d| format!("- {} → {} ({:.2})", d.our_element, d.rust_equivalent, d.similarity)).collect::<Vec<_>>().join("\n")
-    }
-    
-    fn format_novel(&self, novel: &[NovelElement]) -> String { 
-        novel.iter().map(|n| format!("- {} (novelty: {:.2})", n.our_element, n.novelty_score)).collect::<Vec<_>>().join("\n")
-    }
-    
-    fn format_missing(&self, missing: &[MissingElement]) -> String { 
-        missing.iter().map(|m| format!("- {} ({})", m.rust_element, m.why_missing)).collect::<Vec<_>>().join("\n")
-    }
-    
-    fn format_improvements(&self, improvements: &[ImprovementSuggestion]) -> String { 
-        improvements.iter().map(|i| format!("- {}: {} → {}", i.file, i.current_code.chars().take(30).collect::<String>(), i.suggested_code.chars().take(30).collect::<String>())).collect::<Vec<_>>().join("\n")
-    }
-    
-    fn format_rewrites(&self, rewrites: &[RewriteRecommendation]) -> String { 
-        rewrites.iter().map(|r| format!("- {}: {}", r.module, r.mathematical_approach)).collect::<Vec<_>>().join("\n")
-    }
-}
diff --git a/compiler/introspector-collector/src/rust_enum_generator.rs b/compiler/introspector-collector/src/rust_enum_generator.rs
deleted file mode 100644
index 90a1d3e0729..00000000000
--- a/compiler/introspector-collector/src/rust_enum_generator.rs
+++ /dev/null
@@ -1,154 +0,0 @@
-use crate::canonical_decl::DeclCanonicalizer;
-use std::collections::HashMap;
-
-pub struct RustEnumMacroGenerator {
-    canonicalizer: DeclCanonicalizer,
-    generated_macros: HashMap<String, String>,
-}
-
-impl RustEnumMacroGenerator {
-    pub fn new() -> Self {
-        Self {
-            canonicalizer: DeclCanonicalizer::new(),
-            generated_macros: HashMap::new(),
-        }
-    }
-    
-    pub fn generate_all_rust_enums(&mut self) {
-        // Generate for all major Rust compiler enums
-        self.generate_hir_enums();
-        self.generate_ast_enums();
-        self.generate_middle_enums();
-        self.generate_def_enums();
-    }
-    
-    fn generate_hir_enums(&mut self) {
-        // rustc_hir enums
-        self.gen_enum_macro("ItemKind", &[
-            "Const", "Static", "Fn", "Macro", "Use", "ExternCrate", 
-            "Mod", "ForeignMod", "GlobalAsm", "TyAlias", "OpaqueTy",
-            "Enum", "Struct", "Union", "Trait", "TraitAlias", "Impl"
-        ]);
-        
-        self.gen_enum_macro("ExprKind", &[
-            "Box", "ConstBlock", "Array", "Call", "MethodCall", "Tup",
-            "Binary", "Unary", "Lit", "Cast", "Type", "DropTemps",
-            "Let", "If", "Loop", "Match", "Closure", "Block", "Assign",
-            "AssignOp", "Field", "Index", "Path", "AddrOf", "Break",
-            "Continue", "Ret", "InlineAsm", "Struct", "Repeat", "Yield"
-        ]);
-        
-        self.gen_enum_macro("PatKind", &[
-            "Wild", "Binding", "Struct", "TupleStruct", "Or", "Path",
-            "Tuple", "Box", "Ref", "Lit", "Range", "Slice"
-        ]);
-        
-        self.gen_enum_macro("TyKind", &[
-            "Slice", "Array", "Ptr", "Rptr", "BareFn", "Never", "Tup",
-            "Path", "OpaqueDef", "TraitObject", "Typeof", "Infer", "Err"
-        ]);
-    }
-    
-    fn generate_ast_enums(&mut self) {
-        // rustc_ast enums
-        self.gen_enum_macro("LitKind", &[
-            "Str", "ByteStr", "Byte", "Char", "Int", "Float", "Bool", "Err"
-        ]);
-        
-        self.gen_enum_macro("BinOpKind", &[
-            "Add", "Sub", "Mul", "Div", "Rem", "And", "Or", "BitXor",
-            "BitAnd", "BitOr", "Shl", "Shr", "Eq", "Lt", "Le", "Ne", "Ge", "Gt"
-        ]);
-        
-        self.gen_enum_macro("UnOpKind", &["Deref", "Not", "Neg"]);
-    }
-    
-    fn generate_middle_enums(&mut self) {
-        // rustc_middle enums
-        self.gen_enum_macro("DefKind", &[
-            "Mod", "Struct", "Union", "Enum", "Variant", "Trait", "TyAlias",
-            "ForeignTy", "TraitAlias", "AssocTy", "AssocFn", "AssocConst",
-            "Fn", "Const", "ConstParam", "Static", "Ctor", "Macro", "ExternCrate",
-            "Use", "ForeignMod", "AnonConst", "InlineConst", "OpaqueTy", "Field",
-            "LifetimeParam", "GlobalAsm", "Impl", "Closure", "Generator"
-        ]);
-    }
-    
-    fn generate_def_enums(&mut self) {
-        self.gen_enum_macro("Mutability", &["Not", "Mut"]);
-        self.gen_enum_macro("Safety", &["Unsafe", "Safe"]);
-        self.gen_enum_macro("Constness", &["Const", "NotConst"]);
-    }
-    
-    fn gen_enum_macro(&mut self, enum_name: &str, variants: &[&str]) {
-        let canonical = self.canonicalizer.canonicalize_enum(enum_name, variants);
-        let macro_name = format!("switch_{}", enum_name.to_lowercase());
-        let string_macro_name = format!("switch_{}2string", enum_name.to_lowercase());
-        
-        let macro_code = format!(r#"
-/// Auto-generated optimized switch for {enum_name}
-/// Content hash: {hash}
-#[macro_export]
-macro_rules! {macro_name} {{
-    ($expr:expr => {{ $($pattern:pat => $result:expr),* $(,)? }}) => {{
-        match $expr {{ $($pattern => $result,)* }}
-    }};
-}}
-
-/// Auto-generated string converter for {enum_name}
-#[macro_export]
-macro_rules! {string_macro_name} {{
-    ($expr:expr) => {{
-        {string_macro_name}!($expr, {{}})
-    }};
-    ($expr:expr, {{ $($pattern:pat => $custom:expr),* $(,)? }}) => {{
-        match $expr {{
-            // Custom overrides first
-            $($pattern => $custom.to_string(),)*
-            // Default string representations
-{default_cases}
-        }}
-    }};
-}}
-"#, 
-            enum_name = enum_name,
-            hash = canonical.hash,
-            macro_name = macro_name,
-            string_macro_name = string_macro_name,
-            default_cases = variants.iter()
-                .map(|v| format!("            _ if matches!($expr, {}::{}) => \"{}\".to_string(),", 
-                    self.get_enum_path(enum_name), v, v))
-                .collect::<Vec<_>>()
-                .join("\n")
-        );
-        
-        self.generated_macros.insert(enum_name.to_string(), macro_code);
-    }
-    
-    fn get_enum_path(&self, enum_name: &str) -> &'static str {
-        match enum_name {
-            "ItemKind" => "rustc_hir::ItemKind",
-            "ExprKind" => "rustc_hir::ExprKind", 
-            "LitKind" => "rustc_ast::LitKind",
-            "DefKind" => "rustc_hir::def::DefKind",
-            _ => "UnknownEnum"
-        }
-    }
-    
-    pub fn save_all_macros(&self, output_dir: &str) -> Result<(), Box<dyn std::error::Error>> {
-        std::fs::create_dir_all(output_dir)?;
-        
-        // Save individual macro files
-        for (enum_name, macro_code) in &self.generated_macros {
-            let filename = format!("{}/switch_{}.rs", output_dir, enum_name.to_lowercase());
-            std::fs::write(&filename, macro_code)?;
-        }
-        
-        // Save combined file
-        let combined = self.generated_macros.values().cloned().collect::<Vec<_>>().join("\n");
-        std::fs::write(format!("{}/all_rust_enum_switches.rs", output_dir), combined)?;
-        
-        println!("Generated {} enum switch macros in {}", self.generated_macros.len(), output_dir);
-        Ok(())
-    }
-}
diff --git a/compiler/introspector-collector/src/rustmap_macros.rs b/compiler/introspector-collector/src/rustmap_macros.rs
deleted file mode 100644
index ce451d4effd..00000000000
--- a/compiler/introspector-collector/src/rustmap_macros.rs
+++ /dev/null
@@ -1,112 +0,0 @@
-/// Macro to map our types to Rust equivalents and generate lifting transformations
-#[macro_export]
-macro_rules! rustmap {
-    // Map type as duplicate
-    (duplicate $our_type:ty => $rust_type:path) => {
-        impl From<$rust_type> for $our_type {
-            fn from(rust_val: $rust_type) -> Self {
-                // Direct isomorphism - types are equivalent
-                unsafe { std::mem::transmute(rust_val) }
-            }
-        }
-        
-        impl Into<$rust_type> for $our_type {
-            fn into(self) -> $rust_type {
-                unsafe { std::mem::transmute(self) }
-            }
-        }
-    };
-    
-    // Map type as equivalent with field lifting
-    (equivalent $our_type:ty => $rust_type:path { $($field:ident),* }) => {
-        impl From<$rust_type> for $our_type {
-            fn from(rust_val: $rust_type) -> Self {
-                Self {
-                    $($field: rust_val.$field.into(),)*
-                }
-            }
-        }
-    };
-    
-    // Map function with projection
-    (function $our_fn:ident => $rust_fn:path) => {
-        pub fn $our_fn<T, R>(input: T) -> R 
-        where 
-            T: Into<$rust_fn::Input>,
-            R: From<$rust_fn::Output>
-        {
-            let rust_input = input.into();
-            let rust_output = $rust_fn(rust_input);
-            rust_output.into()
-        }
-    };
-    
-    // Map enum with variant lifting
-    (enum $our_enum:ty => $rust_enum:path { 
-        $($variant:ident $(($($field:ty),*))? ),* 
-    }) => {
-        impl From<$rust_enum> for $our_enum {
-            fn from(rust_val: $rust_enum) -> Self {
-                match rust_val {
-                    $(
-                        $rust_enum::$variant $(($(ref $field),*))? => {
-                            Self::$variant $(($((*$field).clone().into()),*))?
-                        }
-                    )*
-                }
-            }
-        }
-    };
-}
-
-/// Macro to generate visitor projections/weaves
-#[macro_export]
-macro_rules! visitor_weave {
-    ($visitor_name:ident : $source_space:ty => $target_space:ty) => {
-        pub struct $visitor_name {
-            projection: Box<dyn Fn($source_space) -> $target_space>,
-        }
-        
-        impl $visitor_name {
-            pub fn new<F>(proj: F) -> Self 
-            where F: Fn($source_space) -> $target_space + 'static 
-            {
-                Self {
-                    projection: Box::new(proj),
-                }
-            }
-            
-            pub fn weave(&self, source: $source_space) -> $target_space {
-                (self.projection)(source)
-            }
-        }
-    };
-}
-
-/// Generate metaprogram for lifting structures
-#[macro_export]
-macro_rules! lift_structure {
-    ($rust_struct:path => $our_struct:ident {
-        $($field:ident: $field_type:ty),*
-    }) => {
-        #[derive(Debug, Clone)]
-        pub struct $our_struct {
-            $(pub $field: $field_type,)*
-        }
-        
-        // rustmap!(equivalent $our_struct => $rust_struct { $($field),* });
-        
-        impl $our_struct {
-            pub fn from_rust_projection(_rust_val: $rust_struct) -> Self {
-                // rust_val.into()
-                Self {
-                    $($field: Default::default(),)*
-                }
-            }
-            
-            pub fn eigenform(&self) -> EigenForm<$our_struct> {
-                EigenForm::new(self.clone())
-            }
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/schema_generator.rs b/compiler/introspector-collector/src/schema_generator.rs
deleted file mode 100644
index 1ecc507a6d0..00000000000
--- a/compiler/introspector-collector/src/schema_generator.rs
+++ /dev/null
@@ -1,164 +0,0 @@
-/// Macro to generate JSON schema from Rust structures
-#[macro_export]
-macro_rules! generate_schema {
-    ($rust_type:ty) => {
-        {
-            let schema = json!({
-                "$schema": "http://json-schema.org/draft-07/schema#",
-                "title": stringify!($rust_type),
-                "type": "object",
-                "properties": schema_properties!($rust_type),
-                "required": required_fields!($rust_type),
-                "additionalProperties": false
-            });
-            schema
-        }
-    };
-}
-
-/// Generate schema properties for any Rust type
-#[macro_export]
-macro_rules! schema_properties {
-    (rustc_hir::Item) => {
-        json!({
-            "owner_id": { "type": "string", "description": "DefId of the item owner" },
-            "ident": { "type": "string", "description": "Item identifier" },
-            "kind": { "$ref": "#/definitions/ItemKind" },
-            "vis_span": { "$ref": "#/definitions/Span" },
-            "span": { "$ref": "#/definitions/Span" },
-            "canonical_hash": { "type": "string", "pattern": "^[a-f0-9]{16}$" }
-        })
-    };
-    
-    (rustc_hir::ItemKind) => {
-        json!({
-            "variant": { 
-                "type": "string",
-                "enum": ["Const", "Static", "Fn", "Macro", "Use", "ExternCrate", 
-                        "Mod", "ForeignMod", "GlobalAsm", "TyAlias", "OpaqueTy",
-                        "Enum", "Struct", "Union", "Trait", "TraitAlias", "Impl"]
-            },
-            "data": { "type": "object", "description": "Variant-specific data" }
-        })
-    };
-    
-    (rustc_hir::Expr) => {
-        json!({
-            "hir_id": { "type": "string" },
-            "kind": { "$ref": "#/definitions/ExprKind" },
-            "span": { "$ref": "#/definitions/Span" }
-        })
-    };
-    
-    (rustc_hir::def::DefKind) => {
-        json!({
-            "variant": {
-                "type": "string", 
-                "enum": ["Mod", "Struct", "Union", "Enum", "Variant", "Trait",
-                        "TyAlias", "ForeignTy", "TraitAlias", "AssocTy", "AssocFn",
-                        "AssocConst", "Fn", "Const", "ConstParam", "Static"]
-            }
-        })
-    };
-}
-
-/// Generate required fields list
-#[macro_export]
-macro_rules! required_fields {
-    (rustc_hir::Item) => {
-        vec!["owner_id", "ident", "kind", "span"]
-    };
-    
-    (rustc_hir::ItemKind) => {
-        vec!["variant"]
-    };
-    
-    ($type:ty) => {
-        vec![]
-    };
-}
-
-/// Generate complete schema with all definitions
-#[macro_export]
-macro_rules! generate_complete_schema {
-    () => {
-        {
-            json!({
-                "$schema": "http://json-schema.org/draft-07/schema#",
-                "title": "Rust Compiler Introspection Schema",
-                "description": "Auto-generated schema matching Rust's internal structures",
-                "type": "object",
-                "properties": {
-                    "crate_name": { "type": "string" },
-                    "items": {
-                        "type": "array",
-                        "items": { "$ref": "#/definitions/Item" }
-                    },
-                    "expressions": {
-                        "type": "array", 
-                        "items": { "$ref": "#/definitions/Expr" }
-                    },
-                    "def_kinds": {
-                        "type": "array",
-                        "items": { "$ref": "#/definitions/DefKind" }
-                    }
-                },
-                "definitions": {
-                    "Item": generate_schema!(rustc_hir::Item),
-                    "ItemKind": generate_schema!(rustc_hir::ItemKind),
-                    "Expr": generate_schema!(rustc_hir::Expr),
-                    "ExprKind": generate_schema!(rustc_hir::ExprKind),
-                    "DefKind": generate_schema!(rustc_hir::def::DefKind),
-                    "Span": {
-                        "type": "object",
-                        "properties": {
-                            "lo": { "type": "integer" },
-                            "hi": { "type": "integer" }
-                        }
-                    },
-                    "StructuredData": {
-                        "type": "object",
-                        "properties": {
-                            "category": { "type": "string" },
-                            "type_name": { "type": "string" },
-                            "canonical_form": { "type": "string" },
-                            "content_hash": { "type": "string", "pattern": "^[a-f0-9]{16}$" },
-                            "fields": {
-                                "type": "array",
-                                "items": { "$ref": "#/definitions/FieldMetadata" }
-                            },
-                            "variants": {
-                                "type": "array", 
-                                "items": { "type": "string" }
-                            }
-                        },
-                        "required": ["category", "type_name", "canonical_form", "content_hash"]
-                    },
-                    "FieldMetadata": {
-                        "type": "object",
-                        "properties": {
-                            "name": { "type": "string" },
-                            "type_name": { "type": "string" },
-                            "size": { "type": "integer", "minimum": 0 },
-                            "alignment": { "type": "integer", "minimum": 1 }
-                        },
-                        "required": ["name", "type_name", "size", "alignment"]
-                    }
-                }
-            })
-        }
-    };
-}
-
-/// Generate schema and save to file
-#[macro_export]
-macro_rules! save_schema {
-    ($path:expr) => {
-        {
-            let schema = generate_complete_schema!();
-            let schema_json = serde_json::to_string_pretty(&schema).unwrap();
-            std::fs::write($path, schema_json).unwrap();
-            println!("Generated JSON schema: {}", $path);
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/self_mapper.rs b/compiler/introspector-collector/src/self_mapper.rs
deleted file mode 100644
index abb44129ef6..00000000000
--- a/compiler/introspector-collector/src/self_mapper.rs
+++ /dev/null
@@ -1,98 +0,0 @@
-use std::collections::HashMap;
-use serde_json;
-use crate::data_structures::*;
-
-pub struct SelfMapper {
-    collector_usage: HashMap<String, UsageEntry>,
-    rust_usage: HashMap<String, UsageEntry>,
-    mappings: HashMap<String, String>, // collector_element -> rust_element
-}
-
-impl SelfMapper {
-    pub fn new() -> Self {
-        Self {
-            collector_usage: HashMap::new(),
-            rust_usage: HashMap::new(),
-            mappings: HashMap::new(),
-        }
-    }
-    
-    pub fn load_self_usage(&mut self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let data = std::fs::read_to_string(path)?;
-        let module_data: ModuleData = serde_json::from_str(&data)?;
-        
-        for usage in module_data.usages {
-            self.collector_usage.insert(usage.symbol.clone(), usage);
-        }
-        Ok(())
-    }
-    
-    pub fn load_rust_usage(&mut self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let data = std::fs::read_to_string(path)?;
-        let module_data: ModuleData = serde_json::from_str(&data)?;
-        
-        for usage in module_data.usages {
-            self.rust_usage.insert(usage.symbol.clone(), usage);
-        }
-        Ok(())
-    }
-    
-    pub fn map_to_rust_eigenspace(&mut self) {
-        println!("=== MAPPING COLLECTOR → RUST EIGENSPACE ===");
-        
-        for (collector_symbol, collector_usage) in &self.collector_usage {
-            // Find similar patterns in Rust usage
-            let rust_match = self.find_rust_analog(collector_symbol, collector_usage);
-            
-            if let Some(rust_symbol) = rust_match {
-                self.mappings.insert(collector_symbol.clone(), rust_symbol.clone());
-                println!("MAPPED: {} → {}", collector_symbol, rust_symbol);
-            } else {
-                println!("NOVEL: {} (no Rust analog found)", collector_symbol);
-            }
-        }
-    }
-    
-    fn find_rust_analog(&self, symbol: &str, usage: &UsageEntry) -> Option<String> {
-        // Look for similar usage patterns in Rust
-        for (rust_symbol, rust_usage) in &self.rust_usage {
-            if self.usage_similarity(usage, rust_usage) > 0.8 {
-                return Some(rust_symbol.clone());
-            }
-        }
-        None
-    }
-    
-    fn usage_similarity(&self, a: &UsageEntry, b: &UsageEntry) -> f64 {
-        let mut score = 0.0;
-        
-        // Compare usage types
-        if a.usage_type == b.usage_type { score += 0.3; }
-        if a.kind == b.kind { score += 0.3; }
-        if a.node_type == b.node_type { score += 0.2; }
-        
-        // Compare usage patterns
-        if a.usage_count == b.usage_count { score += 0.2; }
-        
-        score
-    }
-    
-    pub fn generate_eigendecomposition(&self) {
-        println!("\n=== EIGENDECOMPOSITION ANALYSIS ===");
-        
-        let mapped_count = self.mappings.len();
-        let novel_count = self.collector_usage.len() - mapped_count;
-        
-        println!("Mapped elements: {} (existing eigenspace)", mapped_count);
-        println!("Novel elements: {} (eigenspace extension)", novel_count);
-        
-        let coverage = mapped_count as f64 / self.collector_usage.len() as f64;
-        println!("Eigenspace coverage: {:.2}%", coverage * 100.0);
-        
-        if coverage < 0.5 {
-            println!("→ Collector extends Rust eigenspace significantly");
-        } else {
-            println!("→ Collector mostly within existing eigenspace");
-        }
-    }
-}
diff --git a/compiler/introspector-collector/src/structured_collection.rs b/compiler/introspector-collector/src/structured_collection.rs
deleted file mode 100644
index aaab8605708..00000000000
--- a/compiler/introspector-collector/src/structured_collection.rs
+++ /dev/null
@@ -1,154 +0,0 @@
-use serde::{Serialize, Deserialize};
-
-#[derive(Debug, Clone, Serialize, Deserialize)]
-pub struct TypeInfo {
-    pub name: String,
-    pub module: String,
-    pub size_hint: Option<usize>,
-}
-
-/// Macro to define structured data collection for any Rust type
-#[macro_export]
-macro_rules! collect_structured {
-    ($collector:expr, $category:expr, $item:expr, $type_name:ident) => {
-        {
-            let structured_data = StructuredData {
-                category: $category.to_string(),
-                type_name: stringify!($type_name).to_string(),
-                canonical_form: canonical_form!($item),
-                content_hash: content_hash!($item),
-                fields: extract_fields!($item),
-                variants: extract_variants!($item),
-                usage_context: current_context!(),
-            };
-            
-            $collector.add_structured_usage(structured_data);
-        }
-    };
-}
-
-/// Macro to extract all fields from any structure
-#[macro_export]
-macro_rules! extract_fields {
-    // For HIR Items
-    (rustc_hir::Item) => {
-        vec![
-            field_data!("owner_id", DefId),
-            field_data!("ident", Ident), 
-            field_data!("kind", ItemKind),
-            field_data!("vis_span", Span),
-            field_data!("span", Span)
-        ]
-    };
-    
-    // For HIR Expressions  
-    (rustc_hir::Expr) => {
-        vec![
-            field_data!("hir_id", HirId),
-            field_data!("kind", ExprKind),
-            field_data!("span", Span)
-        ]
-    };
-    
-    // For DefKind (enum)
-    (rustc_hir::def::DefKind) => {
-        vec![
-            field_data!("discriminant", u8),
-            field_data!("variant_data", VariantData)
-        ]
-    };
-    
-    // Generic fallback
-    ($item:expr) => {
-        extract_fields_generic!($item)
-    };
-}
-
-/// Macro to extract variants from enums
-#[macro_export]
-macro_rules! extract_variants {
-    (rustc_hir::ItemKind) => {
-        vec![
-            "Const", "Static", "Fn", "Macro", "Use", "ExternCrate",
-            "Mod", "ForeignMod", "GlobalAsm", "TyAlias", "OpaqueTy", 
-            "Enum", "Struct", "Union", "Trait", "TraitAlias", "Impl"
-        ].into_iter().map(|s| s.to_string()).collect()
-    };
-    
-    (rustc_hir::ExprKind) => {
-        vec![
-            "Box", "ConstBlock", "Array", "Call", "MethodCall", "Tup",
-            "Binary", "Unary", "Lit", "Cast", "Type", "DropTemps",
-            "Let", "If", "Loop", "Match", "Closure", "Block", "Assign"
-        ].into_iter().map(|s| s.to_string()).collect()
-    };
-    
-    ($item:expr) => { vec![] };
-}
-
-/// Macro to create field metadata
-#[macro_export]
-macro_rules! field_data {
-    ($name:expr, $type:ty) => {
-        FieldMetadata {
-            name: $name.to_string(),
-            type_name: stringify!($type).to_string(),
-            size: std::mem::size_of::<$type>(),
-            alignment: std::mem::align_of::<$type>(),
-        }
-    };
-}
-
-/// Macro to wrap any Rust structure for collection
-#[macro_export]
-macro_rules! wrap_for_collection {
-    ($item:expr, $category:expr) => {
-        {
-            let wrapper = DataWrapper {
-                category: $category.to_string(),
-                raw_data: format!("{:?}", $item),
-                type_info: TypeInfo::from_type(&$item),
-                canonical_hash: content_hash!(canonical_form!($item)),
-                collection_timestamp: std::time::SystemTime::now(),
-            };
-            wrapper
-        }
-    };
-}
-
-/// Unified data structure for all collected items
-#[derive(Debug, Clone, Serialize, Deserialize)]
-pub struct StructuredData {
-    pub category: String,           // "hir_item", "ast_expr", "def_kind", etc.
-    pub type_name: String,          // "ItemKind", "ExprKind", etc.
-    pub canonical_form: String,     // Minimal canonical representation
-    pub content_hash: String,       // Content-addressable hash
-    pub fields: Vec<FieldMetadata>, // All field information
-    pub variants: Vec<String>,      // For enums: all variants
-    pub usage_context: UsageContext, // Where/how it's used
-}
-
-#[derive(Debug, Clone, Serialize, Deserialize)]
-pub struct FieldMetadata {
-    pub name: String,
-    pub type_name: String,
-    pub size: usize,
-    pub alignment: usize,
-}
-
-#[derive(Debug, Clone, Serialize, Deserialize)]
-pub struct DataWrapper {
-    pub category: String,
-    pub raw_data: String,
-    pub type_info: TypeInfo,
-    pub canonical_hash: String,
-    pub collection_timestamp: std::time::SystemTime,
-}
-
-#[derive(Debug, Clone, Serialize, Deserialize)]
-pub struct UsageContext {
-    pub module: String,
-    pub function: String,
-    pub line: u32,
-    pub frequency: u64,
-}
diff --git a/compiler/introspector-collector/src/stylish_switches.rs b/compiler/introspector-collector/src/stylish_switches.rs
deleted file mode 100644
index 3a96f51d5e8..00000000000
--- a/compiler/introspector-collector/src/stylish_switches.rs
+++ /dev/null
@@ -1,59 +0,0 @@
-/// Stylish switch macro for ItemKind - will be data-driven and optimized
-#[macro_export]
-macro_rules! switch_item_kind {
-    ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-        {
-            // TODO: This will be replaced with data-driven optimization
-            // For now, it's a regular match but marked for transformation
-            match $expr {
-                $($pattern => $result,)*
-            }
-        }
-    };
-}
-
-/// Stylish switch macro for body value kind - optimized for expression patterns  
-#[macro_export]
-macro_rules! switch_body_value_kind {
-    ($expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-        {
-            // TODO: This will be replaced with frequency-based optimization
-            // Expressions will be reordered based on usage data
-            match $expr {
-                $($pattern => $result,)*
-            }
-        }
-    };
-}
-
-/// Generic stylish switch - can be specialized for any type
-#[macro_export]
-macro_rules! switch_with_style {
-    ($switch_type:ident, $expr:expr => { $($pattern:pat => $result:expr),* $(,)? }) => {
-        {
-            // This will load optimization data for the specific switch type
-            // and generate the most efficient implementation
-            match $expr {
-                $($pattern => $result,)*
-            }
-        }
-    };
-}
-
-/// Usage classification switch - will be optimized based on string frequency
-#[macro_export]
-macro_rules! switch_usage_classification {
-    ($usage:expr, $usage_type:expr => { $($condition:expr => $action:expr),* $(,)? }) => {
-        {
-            // TODO: Reorder conditions based on usage frequency data
-            $(
-                if $condition {
-                    $action
-                } else
-            )*
-            {
-                // Default case
-            }
-        }
-    };
-}
diff --git a/compiler/introspector-collector/src/switch_bootstrap.rs b/compiler/introspector-collector/src/switch_bootstrap.rs
deleted file mode 100644
index 9e47c116eb2..00000000000
--- a/compiler/introspector-collector/src/switch_bootstrap.rs
+++ /dev/null
@@ -1,100 +0,0 @@
-/// Bootstrap switch macros - minimal stubs for missing generators
-/// These will be replaced by the full generator system
-
-#[macro_export]
-macro_rules! bootstrap_switch_litkind2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_ast::LitKind::Str(_, _) => ("String", "str"),
-            rustc_ast::LitKind::Int(_, _) => ("Integer", "int"), 
-            rustc_ast::LitKind::Float(_, _) => ("Float", "float"),
-            rustc_ast::LitKind::Bool(_) => ("Boolean", "bool"),
-            rustc_ast::LitKind::Char(_) => ("Character", "char"),
-            _ => ("Other", "other")
-        }
-    };
-    ($expr:expr) => {
-        match $expr {
-            rustc_ast::LitKind::Str(_, _) => ("String", "str"),
-            rustc_ast::LitKind::Int(_, _) => ("Integer", "int"),
-            rustc_ast::LitKind::Float(_, _) => ("Float", "float"), 
-            rustc_ast::LitKind::Bool(_) => ("Boolean", "bool"),
-            rustc_ast::LitKind::Char(_) => ("Character", "char"),
-            _ => ("Other", "other")
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! bootstrap_switch_litinttype2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I8) => "i8".to_string(),
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I16) => "i16".to_string(),
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I32) => "i32".to_string(),
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I64) => "i64".to_string(),
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I128) => "i128".to_string(),
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::Isize) => "isize".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U8) => "u8".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U16) => "u16".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U32) => "u32".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U64) => "u64".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U128) => "u128".to_string(),
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::Usize) => "usize".to_string(),
-            rustc_ast::LitIntType::Unsuffixed => "unsuffixed".to_string(),
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! bootstrap_switch_litfloattype2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F32) => "f32".to_string(),
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F64) => "f64".to_string(),
-            rustc_ast::LitFloatType::Unsuffixed => "unsuffixed_float".to_string(),
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! bootstrap_switch_itemkind {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => $custom_result,)*
-            rustc_hir::ItemKind::Fn { .. } => "Fn",
-            rustc_hir::ItemKind::Struct(..) => "Struct",
-            rustc_hir::ItemKind::Enum(..) => "Enum",
-            rustc_hir::ItemKind::Const(..) => "Const",
-            rustc_hir::ItemKind::Static(..) => "Static",
-            rustc_hir::ItemKind::Trait(..) => "Trait",
-            rustc_hir::ItemKind::Impl(..) => "Impl",
-            rustc_hir::ItemKind::Mod(..) => "Mod",
-            rustc_hir::ItemKind::Use(..) => "Use",
-            _ => "Other"
-        }
-    };
-}
-
-#[macro_export]
-macro_rules! bootstrap_switch_itemkind_custom {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        bootstrap_switch_itemkind!($expr, { $($custom_pattern => $custom_result),* })
-    };
-}
-
-#[macro_export]
-macro_rules! bootstrap_switch_node2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => ($custom_result).to_string(),)*
-            _ => format!("{:?}", $expr)
-        }
-    };
-    ($expr:expr) => {
-        format!("{:?}", $expr)
-    };
-}
diff --git a/compiler/introspector-collector/src/syn_macro_generator.rs b/compiler/introspector-collector/src/syn_macro_generator.rs
deleted file mode 100644
index 6b9bddcf7d5..00000000000
--- a/compiler/introspector-collector/src/syn_macro_generator.rs
+++ /dev/null
@@ -1,271 +0,0 @@
-use syn::{parse_quote, ItemEnum, Variant, Fields};
-use quote::quote;
-use proc_macro2::TokenStream;
-use std::fs;
-
-pub struct SynMacroGenerator {
-    generated_macros: Vec<TokenStream>,
-}
-
-impl SynMacroGenerator {
-    pub fn new() -> Self {
-        Self {
-            generated_macros: Vec::new(),
-        }
-    }
-    
-    /// Generate switch macro for a Rust enum using syn
-    pub fn generate_enum_switch_macro(&mut self, enum_name: &str, macro_name: &str, rust_path: &str) {
-        // Create a synthetic enum definition for common Rust compiler enums
-        let enum_def = match enum_name {
-            "ItemKind" => parse_quote! {
-                pub enum ItemKind {
-                    Fn,
-                    Struct,
-                    Enum,
-                    Const,
-                    Static,
-                    Trait,
-                    Impl,
-                    Mod,
-                    Use,
-                    TyAlias,
-                    Union,
-                }
-            },
-            "DefKind" => parse_quote! {
-                pub enum DefKind {
-                    Fn,
-                    AssocFn,
-                    Struct,
-                    Variant,
-                    Const,
-                    AssocConst,
-                    Static,
-                    Trait,
-                    TyAlias,
-                    Mod,
-                    Field,
-                }
-            },
-            "ExprKind" => parse_quote! {
-                pub enum ExprKind {
-                    Lit,
-                    Call,
-                    Match,
-                    If,
-                    Loop,
-                    Block,
-                    Assign,
-                    Binary,
-                    Unary,
-                    Cast,
-                    Path,
-                    AddrOf,
-                    Break,
-                    Continue,
-                    Ret,
-                }
-            },
-            "Mutability" => parse_quote! {
-                pub enum Mutability {
-                    Mut,
-                    Not,
-                }
-            },
-            "LitKind" => parse_quote! {
-                pub enum LitKind {
-                    Str,
-                    ByteStr,
-                    Byte,
-                    Char,
-                    Int,
-                    Float,
-                    Bool,
-                    Err,
-                }
-            },
-            "Node" => parse_quote! {
-                pub enum Node {
-                    Item,
-                    Expr,
-                    Stmt,
-                    TraitItem,
-                    ImplItem,
-                    ForeignItem,
-                    Field,
-                    AnonConst,
-                    Ctor,
-                    Pat,
-                    Arm,
-                    Block,
-                    Local,
-                    Param,
-                    GenericParam,
-                    Crate,
-                }
-            },
-            _ => {
-                println!("Unknown enum: {}", enum_name);
-                return;
-            }
-        };
-        
-        self.generate_switch_from_enum(&enum_def, macro_name, rust_path);
-    }
-    
-    fn generate_switch_from_enum(&mut self, enum_def: &ItemEnum, macro_name: &str, rust_path: &str) {
-        let macro_ident = syn::Ident::new(macro_name, proc_macro2::Span::call_site());
-        
-        // Generate match arms for each variant
-        let mut default_arms = Vec::new();
-        
-        for variant in &enum_def.variants {
-            let variant_ident = &variant.ident;
-            let variant_name = variant_ident.to_string();
-            
-            // Handle different field types
-            let pattern = match &variant.fields {
-                Fields::Named(_) => quote! { #rust_path::#variant_ident { .. } },
-                Fields::Unnamed(_) => quote! { #rust_path::#variant_ident(..) },
-                Fields::Unit => quote! { #rust_path::#variant_ident },
-            };
-            
-            default_arms.push(quote! {
-                #pattern => #variant_name.to_string()
-            });
-        }
-        
-        // Generate the macro
-        let generated_macro = quote! {
-            /// Auto-generated switch macro using syn
-            #[macro_export]
-            macro_rules! #macro_ident {
-                ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-                    match $expr {
-                        $($custom_pattern => ($custom_result).to_string(),)*
-                        #(#default_arms,)*
-                        _ => "Other".to_string()
-                    }
-                };
-                ($expr:expr) => {
-                    match $expr {
-                        #(#default_arms,)*
-                        _ => "Other".to_string()
-                    }
-                };
-            }
-        };
-        
-        self.generated_macros.push(generated_macro);
-    }
-    
-    pub fn generate_all_rust_enum_macros(&mut self) {
-        println!("🔧 Generating enum switch macros with syn...");
-        
-        // Generate macros for all major Rust compiler enums
-        self.generate_enum_switch_macro("ItemKind", "switch_itemkind2string", "rustc_hir::ItemKind");
-        self.generate_enum_switch_macro("DefKind", "switch_defkind2string", "rustc_hir::def::DefKind");
-        self.generate_enum_switch_macro("ExprKind", "switch_exprkind2string", "rustc_hir::ExprKind");
-        self.generate_enum_switch_macro("Mutability", "switch_mutability2string", "rustc_hir::Mutability");
-        self.generate_enum_switch_macro("LitKind", "switch_litkind2string", "rustc_ast::LitKind");
-        self.generate_enum_switch_macro("Node", "switch_node2string", "rustc_hir::Node");
-        
-        println!("✅ Generated {} enum switch macros", self.generated_macros.len());
-    }
-    
-    pub fn save_generated_macros(&self, output_path: &str) -> Result<(), std::io::Error> {
-        let mut output = String::new();
-        
-        output.push_str("// Auto-generated by syn-based macro generator\n");
-        output.push_str("// DO NOT EDIT - Generated at build time\n\n");
-        
-        for macro_tokens in &self.generated_macros {
-            output.push_str(&macro_tokens.to_string());
-            output.push_str("\n\n");
-        }
-        
-        // Add special helper macros
-        output.push_str(&self.generate_special_macros());
-        
-        fs::write(output_path, output)?;
-        println!("💾 Saved generated macros to: {}", output_path);
-        Ok(())
-    }
-    
-    fn generate_special_macros(&self) -> String {
-        r#"/// Special version for custom pattern matching with ItemKind
-#[macro_export]
-macro_rules! switch_itemkind_custom {
-    ($expr:expr, { $($pattern:pat => $body:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $body,)*
-            _ => switch_itemkind2string!($expr)
-        }
-    };
-}
-
-/// Pattern matching switch for ItemKind - executes custom code blocks
-#[macro_export]
-macro_rules! switch_itemkind {
-    ($expr:expr => { $($pattern:pat => $body:block)* }) => {
-        match $expr {
-            $($pattern => $body)*
-        }
-    };
-}
-
-/// Helper macro for literal types with tuple returns
-#[macro_export]
-macro_rules! switch_litkind2string {
-    ($expr:expr, { $($custom_pattern:pat => $custom_result:expr),* $(,)? }) => {
-        match $expr {
-            $($custom_pattern => $custom_result,)*
-            rustc_ast::LitKind::Str(..) => ("String".to_string(), "str_"),
-            rustc_ast::LitKind::Int(..) => ("Int".to_string(), "int_"),
-            rustc_ast::LitKind::Bool(..) => ("Bool".to_string(), "bool_"),
-            rustc_ast::LitKind::Float(..) => ("Float".to_string(), "float_"),
-            rustc_ast::LitKind::Char(..) => ("Char".to_string(), "char_"),
-            _ => ("Other".to_string(), "other_")
-        }
-    };
-}
-
-/// Integer type switch macro
-#[macro_export]
-macro_rules! switch_litinttype2string {
-    ($expr:expr, { $($pattern:pat => $custom:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $custom,)*
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::Isize) => "isize_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I8) => "i8_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I16) => "i16_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I32) => "i32_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I64) => "i64_",
-            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I128) => "i128_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::Usize) => "usize_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U8) => "u8_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U16) => "u16_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U32) => "u32_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U64) => "u64_",
-            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U128) => "u128_",
-            rustc_ast::LitIntType::Unsuffixed => "unsuffixed_",
-        }
-    };
-}
-
-/// Float type switch macro
-#[macro_export]
-macro_rules! switch_litfloattype2string {
-    ($expr:expr, { $($pattern:pat => $custom:expr),* $(,)? }) => {
-        match $expr {
-            $($pattern => $custom,)*
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F32) => "f32_",
-            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F64) => "f64_",
-            rustc_ast::LitFloatType::Unsuffixed => "float_unsuffixed_",
-        }
-    };
-}
-"#.to_string()
-    }
-}
diff --git a/compiler/introspector-collector/src/typespace_mapper.rs b/compiler/introspector-collector/src/typespace_mapper.rs
deleted file mode 100644
index 35fd5db32cf..00000000000
--- a/compiler/introspector-collector/src/typespace_mapper.rs
+++ /dev/null
@@ -1,175 +0,0 @@
-use std::collections::HashMap;
-use serde::{Serialize, Deserialize};
-use crate::data_structures::*;
-
-#[derive(Serialize, Deserialize, Debug, Clone)]
-pub struct TypespaceMapping {
-    pub collector_element: String,
-    pub element_type: String, // "type", "function", "const", "macro"
-    pub rust_matches: Vec<RustMatch>,
-    pub similarity_score: f64,
-}
-
-#[derive(Serialize, Deserialize, Debug, Clone)]
-pub struct RustMatch {
-    pub rust_element: String,
-    pub similarity: f64,
-    pub match_type: String,
-    pub usage_pattern: String,
-}
-
-pub struct TypespaceMapper {
-    collector_types: HashMap<String, UsageEntry>,
-    rust_types: HashMap<String, UsageEntry>,
-    mappings: Vec<TypespaceMapping>,
-}
-
-impl TypespaceMapper {
-    pub fn new() -> Self {
-        Self {
-            collector_types: HashMap::new(),
-            rust_types: HashMap::new(),
-            mappings: Vec::new(),
-        }
-    }
-    
-    pub fn map_typespace(&mut self) {
-        println!("=== TYPESPACE MAPPING: T_collector → T_rust ===");
-        
-        for (element_name, usage) in &self.collector_types {
-            let element_type = self.classify_element(&usage.kind);
-            let rust_matches = self.find_closest_3(element_name, usage);
-            
-            let avg_similarity = rust_matches.iter()
-                .map(|m| m.similarity)
-                .sum::<f64>() / rust_matches.len().max(1) as f64;
-            
-            let mapping = TypespaceMapping {
-                collector_element: element_name.clone(),
-                element_type,
-                rust_matches,
-                similarity_score: avg_similarity,
-            };
-            
-            self.print_mapping(&mapping);
-            self.mappings.push(mapping);
-        }
-    }
-    
-    fn classify_element(&self, kind: &str) -> String {
-        match kind {
-            k if k.contains("struct") => "type".to_string(),
-            k if k.contains("enum") => "type".to_string(),
-            k if k.contains("function") => "function".to_string(),
-            k if k.contains("const") => "const".to_string(),
-            k if k.contains("macro") => "macro".to_string(),
-            _ => "other".to_string(),
-        }
-    }
-    
-    fn find_closest_3(&self, element: &str, usage: &UsageEntry) -> Vec<RustMatch> {
-        let mut candidates: Vec<RustMatch> = Vec::new();
-        
-        for (rust_element, rust_usage) in &self.rust_types {
-            let similarity = self.calculate_similarity(usage, rust_usage);
-            
-            candidates.push(RustMatch {
-                rust_element: rust_element.clone(),
-                similarity,
-                match_type: self.get_match_type(usage, rust_usage),
-                usage_pattern: rust_usage.usage_type.clone(),
-            });
-        }
-        
-        // Sort by similarity and take top 3
-        candidates.sort_by(|a, b| b.similarity.partial_cmp(&a.similarity).unwrap());
-        candidates.truncate(3);
-        candidates
-    }
-    
-    fn calculate_similarity(&self, a: &UsageEntry, b: &UsageEntry) -> f64 {
-        let mut score = 0.0;
-        
-        // Exact matches
-        if a.kind == b.kind { score += 0.4; }
-        if a.usage_type == b.usage_type { score += 0.3; }
-        if a.node_type == b.node_type { score += 0.2; }
-        
-        // Fuzzy matches
-        score += self.string_similarity(&a.symbol, &b.symbol) * 0.1;
-        
-        score
-    }
-    
-    fn string_similarity(&self, a: &str, b: &str) -> f64 {
-        let a_lower = a.to_lowercase();
-        let b_lower = b.to_lowercase();
-        
-        if a_lower == b_lower { return 1.0; }
-        if a_lower.contains(&b_lower) || b_lower.contains(&a_lower) { return 0.7; }
-        
-        // Simple character overlap
-        let common: usize = a_lower.chars()
-            .filter(|c| b_lower.contains(*c))
-            .count();
-        
-        common as f64 / a_lower.len().max(b_lower.len()) as f64
-    }
-    
-    fn get_match_type(&self, a: &UsageEntry, b: &UsageEntry) -> String {
-        if a.kind == b.kind && a.usage_type == b.usage_type {
-            "exact".to_string()
-        } else if a.kind == b.kind {
-            "structural".to_string()
-        } else if a.usage_type == b.usage_type {
-            "behavioral".to_string()
-        } else {
-            "fuzzy".to_string()
-        }
-    }
-    
-    fn print_mapping(&self, mapping: &TypespaceMapping) {
-        println!("\n{} [{}] → Rust typespace:", 
-                mapping.collector_element, 
-                mapping.element_type);
-        
-        for (i, rust_match) in mapping.rust_matches.iter().enumerate() {
-            println!("  {}. {} ({:.3}) [{}] {}", 
-                    i + 1,
-                    rust_match.rust_element,
-                    rust_match.similarity,
-                    rust_match.match_type,
-                    rust_match.usage_pattern);
-        }
-    }
-    
-    pub fn save_mappings(&self, path: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let json = serde_json::to_string_pretty(&self.mappings)?;
-        std::fs::write(path, json)?;
-        println!("\nTypespace mappings saved to: {}", path);
-        Ok(())
-    }
-    
-    pub fn analyze_coverage(&self) {
-        println!("\n=== TYPESPACE COVERAGE ANALYSIS ===");
-        
-        let high_similarity = self.mappings.iter()
-            .filter(|m| m.similarity_score > 0.8)
-            .count();
-        
-        let medium_similarity = self.mappings.iter()
-            .filter(|m| m.similarity_score > 0.5 && m.similarity_score <= 0.8)
-            .count();
-        
-        let low_similarity = self.mappings.iter()
-            .filter(|m| m.similarity_score <= 0.5)
-            .count();
-        
-        println!("High similarity (>0.8): {} elements", high_similarity);
-        println!("Medium similarity (0.5-0.8): {} elements", medium_similarity);
-        println!("Low similarity (<0.5): {} elements (novel)", low_similarity);
-        
-        let novelty_ratio = low_similarity as f64 / self.mappings.len() as f64;
-        println!("Novelty ratio: {:.2}%", novelty_ratio * 100.0);
-    }
-}
diff --git a/compiler/introspector-collector/src/usage_collector.rs b/compiler/introspector-collector/src/usage_collector.rs
index 6affe9a4a57..d0014ef878c 100644
--- a/compiler/introspector-collector/src/usage_collector.rs
+++ b/compiler/introspector-collector/src/usage_collector.rs
@@ -9,7 +9,6 @@
 use crate::data_structures::*;
 use crate::file_manager::FileManager;
 use crate::collectors::*;
-use crate::{switch_itemkind2string, switch_defkind2string, switch_litkind2string, switch_itemkind, switch_itemkind_custom, switch_node2string, dwim};
 
 use rustc_driver::{Callbacks, Compilation};
 use rustc_interface::interface;
@@ -66,15 +65,26 @@ pub fn add_usage(
     }
     
     pub fn get_defkind_from_item<'tcx>(&self, tcx: TyCtxt<'tcx>, item: &rustc_hir::Item) -> String {
-        switch_itemkind_custom!(&item.kind, {
+        match &item.kind {
             rustc_hir::ItemKind::Const(_, _, _, body_id) => {
                 let body = tcx.hir_body(*body_id);
                 match &body.value.kind {
-                    rustc_hir::ExprKind::Lit(lit) => format!("Const_{}", switch_litkind2string!(lit.node, {}).0),
-                    _ => "Const".to_string()
+                    rustc_hir::ExprKind::Lit(lit) => match lit.node {
+                        rustc_ast::LitKind::Str(..) => "StringLiteral",
+                        rustc_ast::LitKind::Int(..) => "IntLiteral", 
+                        rustc_ast::LitKind::Float(..) => "FloatLiteral",
+                        rustc_ast::LitKind::Bool(..) => "BoolLiteral",
+                        _ => "Const"
+                    },
+                    _ => "Const"
                 }
             },
-        })
+            rustc_hir::ItemKind::Static(..) => "Static",
+            rustc_hir::ItemKind::Struct(..) => "Struct",
+            rustc_hir::ItemKind::Enum(..) => "Enum",
+            rustc_hir::ItemKind::Fn { .. } => "Fn",
+            _ => "Other"
+        }.to_string()
     }
     
     pub fn classify_usage(&self, usage: &str, usage_type: &str, _used_def_id: &str) -> UsageClassification {
@@ -181,43 +191,33 @@ pub fn collect_constants<'tcx>(&mut self, tcx: TyCtxt<'tcx>) {
         for item_id in tcx.hir_crate_items(()).free_items() {
             let item = tcx.hir_expect_item(item_id.owner_id.def_id);
             
-            let item_name = tcx.item_name(item.owner_id.to_def_id()).to_string();
-            let item_kind_str = switch_itemkind2string!(&item.kind);
-            
-            // Collect ALL item kinds - no skips
-            self.add_usage(
-                "item_kinds",
-                item_kind_str.clone(),
-                "item_kind".to_string(),
-                "ItemDeclaration".to_string(),
-                "Item".to_string(),
-                crate_name.clone(),
-                item_name.clone(),
-                Some(crate_name.clone()),
-                None,
-                Some(item_kind_str),
-                Some("Item".to_string())
-            );
+            let item_name = match &item.kind {
+                rustc_hir::ItemKind::Use(..) => continue,
+                rustc_hir::ItemKind::ExternCrate(..) => continue,
+                rustc_hir::ItemKind::Impl(..) => continue,
+                rustc_hir::ItemKind::ForeignMod { .. } => continue,
+                _ => tcx.item_name(item.owner_id.to_def_id()).to_string(),
+            };
             
-            // switch_itemkind!(&item.kind => {
-            //     rustc_hir::ItemKind::Const(_, _, _, body_id) => {
-            //         let const_defkind = self.get_defkind_from_item(tcx, item);
-            //         self.add_usage(
-            //             "constants",
-            //             item_name.clone(),
-            //             "const_decl".to_string(),
-            //             "ConstDecl".to_string(),
-            //             "Item".to_string(),
-            //             crate_name.clone(),
-            //             item_name.clone(),
-            //             Some(crate_name.clone()),
-            //             None,
-            //             Some(const_defkind),
-            //             Some("Const".to_string())
-            //         );
-            //     }
-            //     _ => {}
-            // }
+            match &item.kind {
+                rustc_hir::ItemKind::Const(_, _, _, body_id) => {
+                    let const_defkind = self.get_defkind_from_item(tcx, item);
+                    self.add_usage(
+                        "constants",
+                        item_name.clone(),
+                        "const_decl".to_string(),
+                        "ConstDecl".to_string(),
+                        "Item".to_string(),
+                        crate_name.clone(),
+                        item_name.clone(),
+                        Some(crate_name.clone()),
+                        None,
+                        Some(const_defkind),
+                        Some("Const".to_string())
+                    );
+                }
+                _ => {}
+            }
         }
     }
 }
@@ -282,13 +282,20 @@ fn after_analysis<'tcx>(
                 if let Ok((def_kind, used_def_id)) = result {
                     let used_crate = tcx.crate_name(used_def_id.krate);
                     let used_path = tcx.def_path_str(*used_def_id);
+                    let _used_location = format!("crate::{}::{}", used_crate, used_path);
                     
-                    // DWIM! - Generate optimal type analysis code
-                    let usage_type = dwim!(analyze_type, tcx, *used_def_id);
-                    
-                    let node_type = switch_node2string!(tcx.hir_node_by_def_id(item_id.owner_id.def_id));
+                    let node_type = match tcx.hir_node_by_def_id(item_id.owner_id.def_id) {
+                        rustc_hir::Node::Item(_) => "Item",
+                        rustc_hir::Node::Expr(_) => "Expr", 
+                        rustc_hir::Node::Stmt(_) => "Stmt",
+                        rustc_hir::Node::TraitItem(_) => "TraitItem",
+                        rustc_hir::Node::ImplItem(_) => "ImplItem",
+                        rustc_hir::Node::Pat(_) => "Pat",
+                        rustc_hir::Node::Ty(_) => "Ty",
+                        _ => "Other",
+                    }.to_string();
                     
-                    let usage_type = switch_defkind2string!(def_kind, {
+                    let usage_type = match def_kind {
                         rustc_hir::def::DefKind::AssocFn => "MethodCall",
                         rustc_hir::def::DefKind::AssocConst => "AssocConstAccess",
                         rustc_hir::def::DefKind::Variant => "EnumVariant", 
@@ -300,10 +307,12 @@ fn after_analysis<'tcx>(
                         rustc_hir::def::DefKind::Macro(_) => "MacroCall",
                         rustc_hir::def::DefKind::TyAlias => "TypeAlias",
                         rustc_hir::def::DefKind::Union => "UnionUsage",
-                        rustc_hir::def::DefKind::Trait => "TraitUsage"
-                    });
+                        rustc_hir::def::DefKind::Trait => "TraitUsage",
+                        _ => "Other",
+                    }.to_string();
                     
-                    let kind = switch_defkind2string!(def_kind, {
+                    let symbol = used_path.split("::").last().unwrap_or(&used_path).to_string();
+                    let kind = match def_kind {
                         rustc_hir::def::DefKind::Variant => "enum_variant_usage",
                         rustc_hir::def::DefKind::Struct => "struct_usage", 
                         rustc_hir::def::DefKind::Enum => "enum_usage",
@@ -319,15 +328,16 @@ fn after_analysis<'tcx>(
                         rustc_hir::def::DefKind::AssocConst => "assoc_const_usage",
                         rustc_hir::def::DefKind::Mod => "module_usage",
                         rustc_hir::def::DefKind::Field => "field_usage",
-                        rustc_hir::def::DefKind::Ctor(_, _) => "constructor_usage"
-                    });
+                        rustc_hir::def::DefKind::Ctor(_, _) => "constructor_usage",
+                        _ => "unknown_usage",
+                    }.to_string();
                     
                     let user_def_id_str = format!("{:?}", item_id.owner_id.to_def_id());
                     let used_def_id_str = format!("{:?}", used_def_id);
                     let used_def_kind_str = format!("{:?}", def_kind);
                     let user_def_kind_str = format!("{:?}", tcx.def_kind(item_id.owner_id.to_def_id()));
                     
-                    self.add_usage(&module, used_path, kind, usage_type, node_type, user_def_id_str, used_def_id_str, Some(local_crate.to_string()), Some(used_crate.to_string()), Some(used_def_kind_str), Some(user_def_kind_str));
+                    self.add_usage(&module, symbol, kind, usage_type, node_type, user_def_id_str, used_def_id_str, Some(local_crate.to_string()), Some(used_crate.to_string()), Some(used_def_kind_str), Some(user_def_kind_str));
                 }
             }
         }
diff --git a/compiler/introspector-collector/src/visitors.rs b/compiler/introspector-collector/src/visitors.rs
index 9033722e3a9..e7789b92ddf 100644
--- a/compiler/introspector-collector/src/visitors.rs
+++ b/compiler/introspector-collector/src/visitors.rs
@@ -5,39 +5,204 @@
 extern crate rustc_ast;
 
 use crate::data_structures::*;
-use crate::usage_collector::UsageCollector;
-use crate::{switch_litkind2string, switch_litinttype2string, switch_litfloattype2string, switch_mutability2string, dwim};
 use rustc_middle::ty::TyCtxt;
 use rustc_hir::intravisit::{self, Visitor};
+use std::collections::HashMap;
+use crate::usage_collector::UsageCollector;
 
-pub struct UsageVisitor<'tcx> {
-    pub tcx: TyCtxt<'tcx>,
-    pub collector: UsageCollector,
-    pub crate_name: String,
-}
-
-impl<'tcx> UsageVisitor<'tcx> {
-    pub fn new(tcx: TyCtxt<'tcx>, crate_name: String) -> Self {
-        Self {
-            tcx,
-            collector: UsageCollector::new(),
-            crate_name,
-        }
-    }
+pub struct LiteralVisitor<'a> {
+    pub collector: &'a mut UsageCollector,
+    pub context: String,
 }
 
-impl<'tcx> Visitor<'tcx> for UsageVisitor<'tcx> {
+impl<'tcx> intravisit::Visitor<'tcx> for LiteralVisitor<'_> {
     fn visit_expr(&mut self, expr: &'tcx rustc_hir::Expr<'tcx>) {
-        // DWIM! - Do What I Mean based on usage data
-        dwim!(visit_expr, self, expr);
+        match &expr.kind {
+            rustc_hir::ExprKind::Lit(lit) => {
+                let (literal_value, type_prefix) = match &lit.node {
+                    rustc_ast::LitKind::Bool(b) => (format!("{}", b), "b"),
+                    rustc_ast::LitKind::Char(c) => (format!("'{}'", c), "c"),
+                    rustc_ast::LitKind::Int(i, ty) => {
+                        let prefix = match ty {
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I8) => "i8_",
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I16) => "i16_",
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I32) => "i32_",
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I64) => "i64_",
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::I128) => "i128_",
+                            rustc_ast::LitIntType::Signed(rustc_ast::IntTy::Isize) => "isize_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U8) => "u8_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U16) => "u16_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U32) => "u32_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U64) => "u64_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::U128) => "u128_",
+                            rustc_ast::LitIntType::Unsigned(rustc_ast::UintTy::Usize) => "usize_",
+                            rustc_ast::LitIntType::Unsuffixed => "int_",
+                        };
+                        (format!("{}", i), prefix)
+                    },
+                    rustc_ast::LitKind::Float(f, ty) => {
+                        let prefix = match ty {
+                            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F32) => "f32_",
+                            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F64) => "f64_",
+                            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F16) => "f16_",
+                            rustc_ast::LitFloatType::Suffixed(rustc_ast::FloatTy::F128) => "f128_",
+                            rustc_ast::LitFloatType::Unsuffixed => "float_",
+                        };
+                        (format!("{}", f), prefix)
+                    },
+                    rustc_ast::LitKind::Str(s, _) => (format!("\"{}\"", s), "str_"),
+                    rustc_ast::LitKind::Byte(b) => (format!("{}", b), "byte_"),
+                    rustc_ast::LitKind::ByteStr(bytes, _) => (format!("b\"{}\"", String::from_utf8_lossy(bytes.as_byte_str())), "bstr_"),
+                    _ => return,
+                };
+                
+                self.collector.add_usage(
+                    "literals", 
+                    format!("{}{}", type_prefix, literal_value),
+                    "literal".to_string(),
+                    "NumericLiteral".to_string(), 
+                    "Expression".to_string(), 
+                    self.context.clone(),
+                    literal_value,
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+            }
+            
+            rustc_hir::ExprKind::Field(expr, field) => {
+                let field_name = field.name.to_string();
+                self.collector.add_usage(
+                    "field_access",
+                    format!("field_{}", field_name),
+                    "field_access".to_string(),
+                    "FieldAccess".to_string(),
+                    "Expression".to_string(),
+                    self.context.clone(),
+                    field_name,
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+                self.visit_expr(expr);
+            }
+            
+            rustc_hir::ExprKind::Call(func, args) => {
+                let arg_count = args.len();
+                self.collector.add_usage(
+                    "function_calls",
+                    format!("call_{}_args", arg_count),
+                    "function_call".to_string(),
+                    "FunctionCall".to_string(),
+                    "Expression".to_string(),
+                    self.context.clone(),
+                    format!("{}_args", arg_count),
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+                self.visit_expr(func);
+                for arg in args.iter() { self.visit_expr(arg); }
+            }
+            
+            rustc_hir::ExprKind::AddrOf(_, mutability, expr) => {
+                let ref_type = match mutability {
+                    rustc_hir::Mutability::Mut => "mut_ref",
+                    rustc_hir::Mutability::Not => "ref",
+                };
+                self.collector.add_usage(
+                    "memory_safety",
+                    format!("addr_{}", ref_type),
+                    "memory_ref".to_string(),
+                    "MemoryRef".to_string(),
+                    "Expression".to_string(),
+                    self.context.clone(),
+                    ref_type.to_string(),
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+                self.visit_expr(expr);
+            }
+            
+            rustc_hir::ExprKind::Index(base, index, _) => {
+                self.collector.add_usage(
+                    "indexing",
+                    "index_access".to_string(),
+                    "index_access".to_string(),
+                    "IndexAccess".to_string(),
+                    "Expression".to_string(),
+                    self.context.clone(),
+                    "array_index".to_string(),
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+                self.visit_expr(base);
+                self.visit_expr(index);
+            }
+            
+            rustc_hir::ExprKind::Match(expr, arms, _) => {
+                let arm_count = arms.len();
+                self.collector.add_usage(
+                    "patterns",
+                    format!("match_{}_arms", arm_count),
+                    "pattern_match".to_string(),
+                    "PatternMatch".to_string(),
+                    "Expression".to_string(),
+                    self.context.clone(),
+                    format!("{}_arms", arm_count),
+                    None,
+                    None,
+                    None,
+                    None,
+                );
+                self.visit_expr(expr);
+                for arm in arms.iter() {
+                    if let Some(_guard) = &arm.guard { 
+                        // TODO: Fix guard access
+                    }
+                    self.visit_expr(&arm.body);
+                }
+            }
+            
+            _ => {}
+        }
         
         intravisit::walk_expr(self, expr);
     }
-    
-    fn visit_item(&mut self, item: &'tcx rustc_hir::Item<'tcx>) {
-        // DWIM! - Do What I Mean for items
-        dwim!(visit_item, self, item);
-        
-        intravisit::walk_item(self, item);
+}
+
+pub struct TypeUsageVisitor<'a, 'tcx> {
+    pub collector: &'a mut UsageCollector,
+    pub crate_name: String,
+    pub tcx: TyCtxt<'tcx>,
+}
+
+impl<'tcx> intravisit::Visitor<'tcx> for TypeUsageVisitor<'_, 'tcx> {
+    fn visit_ty(&mut self, ty: &'tcx rustc_hir::Ty<'tcx, rustc_hir::AmbigArg>) {
+        match &ty.kind {
+            rustc_hir::TyKind::Path(rustc_hir::QPath::Resolved(_, path)) => {
+                if let Some(def_id) = path.res.opt_def_id() {
+                    let def_kind = self.tcx.def_kind(def_id);
+                    match def_kind {
+                        rustc_hir::def::DefKind::Struct | 
+                        rustc_hir::def::DefKind::Enum |
+                        rustc_hir::def::DefKind::Union => {
+                            let _symbol = self.tcx.item_name(def_id).to_string();
+                            // Record type usage
+                        }
+                        _ => {}
+                    }
+                }
+            }
+            _ => {}
+        }
+        intravisit::walk_ty(self, ty);
     }
 }
diff --git a/compiler/introspector-macros/Cargo.toml b/compiler/introspector-macros/Cargo.toml
deleted file mode 100644
index f381d875a81..00000000000
--- a/compiler/introspector-macros/Cargo.toml
+++ /dev/null
@@ -1,13 +0,0 @@
-[package]
-name = "introspector-macros"
-version = "0.1.0"
-edition = "2021"
-
-[lib]
-proc-macro = true
-
-[dependencies]
-proc-macro2 = "1.0"
-quote = "1.0"
-syn = { version = "2.0", features = ["full"] }
-serde_json = "1.0"
diff --git a/compiler/introspector-macros/src/lib.rs b/compiler/introspector-macros/src/lib.rs
deleted file mode 100644
index d26db959de9..00000000000
--- a/compiler/introspector-macros/src/lib.rs
+++ /dev/null
@@ -1,130 +0,0 @@
-use proc_macro::TokenStream;
-use quote::quote;
-use syn::{parse_macro_input, LitStr, ExprMatch};
-use std::collections::HashMap;
-
-/// Macro that reads usage data at compile time and generates optimized code
-#[proc_macro]
-pub fn usage_optimize(input: TokenStream) -> TokenStream {
-    let json_path = parse_macro_input!(input as LitStr);
-    
-    // Read usage data at compile time
-    let usage_data = std::fs::read_to_string(json_path.value())
-        .expect("Failed to read usage data");
-
-    let usage: serde_json::Value = serde_json::from_str(&usage_data)
-        .expect("Failed to parse usage JSON");
-
-    let binding = vec![];
-    let usages = usage["usages"].as_array().unwrap_or(&binding);
-    
-    let mut hot_paths = HashMap::new();
-    let mut balanced_paths = HashMap::new();
-    let mut simple_paths = HashMap::new();
-    
-    for usage_entry in usages {
-        let symbol = usage_entry["symbol"].as_str().unwrap_or("unknown");
-        let kind = usage_entry["kind"].as_str().unwrap_or("unknown");
-        let frequency = usage_entry["frequency"].as_u64().unwrap_or(0);
-        
-        if frequency > 1000 {
-            hot_paths.insert(symbol.to_string(), kind.to_string());
-        } else if frequency > 100 {
-            balanced_paths.insert(symbol.to_string(), kind.to_string());
-        } else {
-            simple_paths.insert(symbol.to_string(), kind.to_string());
-        }
-    }
-    
-    let hot_impls: Vec<_> = hot_paths.iter()
-        .map(|(symbol, _kind)| generate_hot_impl(symbol, _kind))
-        .collect();
-    
-    let balanced_impls: Vec<_> = balanced_paths.iter()
-        .map(|(symbol, _kind)| generate_balanced_impl(symbol, _kind))
-        .collect();
-    
-    let simple_impls: Vec<_> = simple_paths.iter()
-        .map(|(symbol, _kind)| generate_simple_impl(symbol, _kind))
-        .collect();
-    
-    let expanded = quote! {
-        #(#hot_impls)*
-        #(#balanced_impls)*
-        #(#simple_impls)*
-    };
-    
-    TokenStream::from(expanded)
-}
-
-fn generate_hot_impl(symbol: &str, _kind: &str) -> proc_macro2::TokenStream {
-    let ident = syn::Ident::new(&format!("hot_{}", symbol), proc_macro2::Span::call_site());
-    quote! {
-        #[inline(always)]
-        pub fn #ident() -> &'static str {
-            #symbol
-        }
-    }
-}
-
-fn generate_balanced_impl(symbol: &str, _kind: &str) -> proc_macro2::TokenStream {
-    let ident = syn::Ident::new(&format!("balanced_{}", symbol), proc_macro2::Span::call_site());
-    quote! {
-        #[inline]
-        pub fn #ident() -> &'static str {
-            #symbol
-        }
-    }
-}
-
-fn generate_simple_impl(symbol: &str, _kind: &str) -> proc_macro2::TokenStream {
-    let ident = syn::Ident::new(&format!("simple_{}", symbol), proc_macro2::Span::call_site());
-    quote! {
-        pub fn #ident() -> &'static str {
-            #symbol
-        }
-    }
-}
-
-/// Smart match optimization based on usage patterns
-#[proc_macro]
-pub fn smart_match(input: TokenStream) -> TokenStream {
-    let match_expr = parse_macro_input!(input as ExprMatch);
-    
-    // Simple passthrough for now
-    let expr = &match_expr.expr;
-    let arms = &match_expr.arms;
-    
-    quote! {
-        match #expr {
-            #(#arms)*
-        }
-    }.into()
-}
-
-/// Generate enum switch based on usage data
-#[proc_macro]
-pub fn enum_switch(input: TokenStream) -> TokenStream {
-    let _match_expr = parse_macro_input!(input as ExprMatch);
-    
-    // For now, just pass through - would need actual enum analysis
-    quote! {
-        // Generated enum switch would go here
-    }.into()
-}
-
-/// Generate jump table for high-frequency matches
-#[proc_macro]
-pub fn jump_table_match(input: TokenStream) -> TokenStream {
-    let match_expr = parse_macro_input!(input as ExprMatch);
-    
-    // Simple passthrough for now
-    let expr = &match_expr.expr;
-    let arms = &match_expr.arms;
-    
-    quote! {
-        match #expr {
-            #(#arms)*
-        }
-    }.into()
-}
diff --git a/compiler/introspector-macros/src/switch_macros.rs b/compiler/introspector-macros/src/switch_macros.rs
deleted file mode 100644
index 838c4d220bc..00000000000
--- a/compiler/introspector-macros/src/switch_macros.rs
+++ /dev/null
@@ -1,173 +0,0 @@
-use proc_macro::TokenStream;
-use quote::quote;
-use syn::{parse_macro_input, Expr, ExprMatch, Pat, Arm};
-
-/// Macro that transforms match statements based on usage data
-#[proc_macro]
-pub fn smart_match(input: TokenStream) -> TokenStream {
-    let match_expr = parse_macro_input!(input as ExprMatch);
-    
-    // Load usage data for this match
-    let usage_data = load_match_usage_data();
-    
-    // Reorder arms based on frequency
-    let optimized_arms = optimize_match_arms(&match_expr.arms, &usage_data);
-    
-    let expr = &match_expr.expr;
-    let optimized = quote! {
-        match #expr {
-            #(#optimized_arms)*
-        }
-    };
-    
-    optimized.into()
-}
-
-/// Macro that generates specialized match statements for enum variants
-#[proc_macro]
-pub fn enum_switch(input: TokenStream) -> TokenStream {
-    let json_path = parse_macro_input!(input as syn::LitStr);
-    
-    let enum_data = load_enum_usage_data(&json_path.value());
-    let switch_impl = generate_enum_switch(&enum_data);
-    
-    switch_impl.into()
-}
-
-/// Macro that creates jump tables for high-frequency switches
-#[proc_macro]
-pub fn jump_table_match(input: TokenStream) -> TokenStream {
-    let match_expr = parse_macro_input!(input as ExprMatch);
-    
-    // Analyze if this should be a jump table
-    if should_use_jump_table(&match_expr) {
-        generate_jump_table(&match_expr)
-    } else {
-        // Fall back to optimized linear search
-        smart_match(input)
-    }
-}
-
-fn optimize_match_arms(arms: &[Arm], usage_data: &std::collections::HashMap<String, u64>) -> Vec<proc_macro2::TokenStream> {
-    let mut arm_frequencies: Vec<_> = arms.iter()
-        .map(|arm| {
-            let pattern_key = extract_pattern_key(&arm.pat);
-            let frequency = usage_data.get(&pattern_key).unwrap_or(&0);
-            (arm, *frequency)
-        })
-        .collect();
-    
-    // Sort by frequency (most common first)
-    arm_frequencies.sort_by(|a, b| b.1.cmp(&a.1));
-    
-    arm_frequencies.into_iter()
-        .map(|(arm, freq)| {
-            let pat = &arm.pat;
-            let guard = &arm.guard;
-            let body = &arm.body;
-            
-            if freq > 1000 {
-                // Hot path - add likely attribute
-                quote! {
-                    #[likely]
-                    #pat #guard => #body,
-                }
-            } else {
-                quote! {
-                    #pat #guard => #body,
-                }
-            }
-        })
-        .collect()
-}
-
-fn generate_enum_switch(enum_data: &serde_json::Value) -> proc_macro2::TokenStream {
-    let variants = enum_data["variants"].as_array().unwrap_or(&vec![]);
-    
-    let match_arms: Vec<_> = variants.iter()
-        .map(|variant| {
-            let name = variant["name"].as_str().unwrap_or("Unknown");
-            let usage_count = variant["usage_count"].as_u64().unwrap_or(0);
-            
-            let variant_ident = syn::Ident::new(name, proc_macro2::Span::call_site());
-            
-            if usage_count > 500 {
-                quote! {
-                    #[hot_path]
-                    Self::#variant_ident => {
-                        // Hot path implementation
-                        self.handle_hot_variant()
-                    }
-                }
-            } else {
-                quote! {
-                    Self::#variant_ident => {
-                        // Standard implementation
-                        self.handle_variant()
-                    }
-                }
-            }
-        })
-        .collect();
-    
-    quote! {
-        match self {
-            #(#match_arms)*
-        }
-    }
-}
-
-fn should_use_jump_table(match_expr: &ExprMatch) -> bool {
-    // Use jump table if we have many integer/enum patterns
-    match_expr.arms.len() > 8 && 
-    match_expr.arms.iter().all(|arm| is_simple_pattern(&arm.pat))
-}
-
-fn generate_jump_table(match_expr: &ExprMatch) -> TokenStream {
-    let expr = &match_expr.expr;
-    let arms = &match_expr.arms;
-    
-    let jump_table_impl = quote! {
-        {
-            // Generate compile-time jump table
-            const JUMP_TABLE: &[fn() -> _] = &[
-                #(|| { #arms.body },)*
-            ];
-            
-            let index = match #expr {
-                #(#arms.pat => /* index */,)*
-            };
-            
-            JUMP_TABLE[index]()
-        }
-    };
-    
-    jump_table_impl.into()
-}
-
-fn extract_pattern_key(pat: &Pat) -> String {
-    match pat {
-        Pat::Ident(ident) => ident.ident.to_string(),
-        Pat::Path(path) => path.path.segments.last()
-            .map(|seg| seg.ident.to_string())
-            .unwrap_or_default(),
-        Pat::Struct(pat_struct) => pat_struct.path.segments.last()
-            .map(|seg| seg.ident.to_string())
-            .unwrap_or_default(),
-        _ => "unknown".to_string(),
-    }
-}
-
-fn is_simple_pattern(pat: &Pat) -> bool {
-    matches!(pat, Pat::Ident(_) | Pat::Path(_) | Pat::Lit(_))
-}
-
-fn load_match_usage_data() -> std::collections::HashMap<String, u64> {
-    // Load from compile-time embedded data
-    std::collections::HashMap::new()
-}
-
-fn load_enum_usage_data(path: &str) -> serde_json::Value {
-    let data = std::fs::read_to_string(path).unwrap_or_default();
-    serde_json::from_str(&data).unwrap_or(serde_json::Value::Null)
-}
diff --git a/src/tools/rustc-perf b/src/tools/rustc-perf
--- a/src/tools/rustc-perf
+++ b/src/tools/rustc-perf
@@ -1 +1 @@
-Subproject commit 5418a5368d5ff0b77983303da39ecce17ae9dd8d
+Subproject commit 5418a5368d5ff0b77983303da39ecce17ae9dd8d-dirty
