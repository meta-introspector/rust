[package]
name = "ontology_aggregator"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "ontology_aggregator"
path = "ontology_aggregator.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
