[package]
name = "ouroboros_compiler"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[workspace]
# Independent workspace for the ouroboros compiler

[build-dependencies]
serde_json = "1.0"

[dependencies]
serde_json = "1.0"
# The compiler will generate its own dependencies from usage data
